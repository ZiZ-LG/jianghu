# 推演坞 · 第 5 刀「坞收敛」施工方案（五层→三层，消灭重复与双口径）

> 状态：**已施工完成（2026-07-02）**。EngineBar 退役（代码留档未删）、E2 迁坞头 `.dock-shift`、列① 吸收姿态解读+全部缺口、信号条只剩风险/弹药、汇总两栏。门禁 80 测全绿；preview 实测背离采纳/忽略链路、三档高度、亮暗、≤900 单列。坞头「最优动作」可选增强未做（best_action 仅 RAISE 时返回 actionKey 非人话，待列④ 接 action-ranking 时一并做）。
> 背景：第 1+2 刀（`6048b54`）后坞共五层，探讨确认 4 组重复 + 1 组口径矛盾。
> 刀序调整：**第 5 刀 → 第 3 刀（AI 预填）→ 第 4 刀减配（上桌按钮）→ M5 复盘态**（先收敛再叠新功能，免在注定拆除的 EngineBar 上白做）。

## 已拍板决策（LG 2026-07-02，不再重问）

1. **E2 背离警示放坞头**（警示行，采纳改分/忽略逻辑原样迁移）。
2. **信号汇总里「关键干系人」清单直接删**（画布牌桌与焦点面板已覆盖）。

## 重复盘点（动刀依据）

| # | 重复 | 现状位置 | 判决 |
|---|---|---|---|
| ① | **双口径矛盾**：老 `lib/pde.ts` analyzeDeal 的 赢面参考/预期回报（EngineBar.tsx:74-76）vs 新 pde-kernel 的赢面（坞头徽章） | EngineBar 局势行 | 老口径删除，**EV/赢面统一 PDE 内核**（坞头徽章为唯一出口） |
| ② | 「局势」四处：坞头药丸(741band)/坞头徽章(四动作)/EngineBar 姿态行/列① 现状锚 | 坞头+EngineBar+列① | 收敛为 坞头一句（徽章+药丸）+ 列① 详情卡（一浅一深） |
| ③ | 「缺口」三处：列① top3 / 信号汇总全部缺口 / 左栏 11 项 | 坞内两处重复 | 列① 放**全部缺口**（列内滚动+挂卡），信号汇总缺口段删 |
| ④ | 「出打法」三口：playbook 推演方案 / 列② AI 顺推 / 右栏参谋 | EngineBar 展开区 | **playbook 退役**（裁决 C，action-ranking 已落地条件满足）；职能由列② 顺推 + 列④ 未来接 action-ranking 承接 |
| ⑤ | 信号汇总「关键干系人」清单 | sb2-summary 第一栏 | 删（拍板 #2） |

## 目标态（五层 → 三层）

```
坞头一行 · 局势句（唯一局势出口）
  ⬆强攻·赢面36%(PDE唯一口径·带置信⚠︎) · 趋赢力45%药丸 · [最优动作 ΔEV top1·可选] · 展开⌃
  🔔 背离警示行（有背离时出现·采纳改分/忽略——E2 迁此，拍板 #1）
四列流水线 · 主链
  ①局势详情(band+打法方向+姿态解读一句+全部缺口·列内滚动·挂卡)
   → ②策略(卡+顺推) → ③倒排(里程碑+终局) → ④行动(第3刀后接 AI 预填；未来接 action-ranking 候选)
坞底 · 旁支 + 对话
  ⚠风险/假设 N · 🎒弹药 N（点开管理·CRUD 保留） | 💬 和地图对话（第1刀成果不动）
```

## 动刀清单

1. **EngineBar 退役**（`EngineBar.tsx` 组件整体移出坞；照惯例代码退役不删，`App`/`DeliberationDock` 不再渲染）：
   - 姿态 `stanceLabel` + 解读 `jointReading/reasonText` → 并入列① 现状锚（一句话解读，title 放依据）；
   - 「预期回报/赢面参考」老口径 → **删除**（重复①）；
   - **E2 背离提案块（EngineBar.tsx:84-93 + acceptShift/dismissed 逻辑）→ 迁坞头警示行**（拍板 #1；`detectShifts` 数据源自 `lib/pde.ts` 保留）；
   - 「推演方案」playbook 展开区（EngineBar.tsx:95-130 + expand/toggle/adopt）→ **随 playbook 退役删除**。
2. **列① 现状锚吸收**：top3 缺口 → 全部缺口（列内滚动）；`…共 N 项缺口` 按钮删；加姿态解读一句。
3. **信号汇总瘦身**：`sb2-signal` 条只剩 `⚠风险 N(·高 M) · 🎒弹药 N · 展开⌄`；展开区 `sb2-summary` 删缺口栏与关键干系人栏（拍板 #2），只留 风险/假设 + 弹药 两栏 CRUD。
4. **老 `lib/pde.ts` 处置**：`detectShifts`（背离）保留供坞头警示行；`analyzeDeal` 的 pWin/EV 路径与 `buildPlaybooks` 废弃（代码不删，坞不再调 `buildPlaybooks`；`analyzeDeal` 若仅剩背离用途可拆薄调用）。
5. **坞头最优动作（可选增强）**：徽章旁加「最优动作：xxx ΔEV+xx」一小段（取 `pdeActions` top1）；若拥挤可放 title，不强求本刀。
6. **样式**：`.eng-*` 系列不再挂载（保留 css 不删）；坞头警示行新样式 `.dock-shift`（黄条，语义变量）。

## 验收

- 坞只剩三层：坞头句（徽章+药丸+可选最优动作+背离警示）/ 四列 / 坞底（风险弹药短条+对话）。
- 全屏**只有一个赢面数字**（PDE 口径）；「预期回报/赢面参考/推演方案」不再出现。
- 背离场景：造证据背离 → 坞头出 🔔 警示 → 采纳改分生效（SET_ROLE）→ 警示消失、徽章/趋赢力重算。
- 缺口只在列①（全部可滚）与左栏；信号条只剩风险/弹药。
- 门禁：`cd app && npx tsc --noEmit && npm run test`（80 测）+ preview（1360 桌面三档高度 + ≤900 单列退化 + 亮暗）。

## 关联

- 裁决 C（playbook 收编退役）：`docs/pde-handoff/DECISIONS.md`
- 第 1-4 刀图纸：`docs/推演坞-横向推导链-施工方案.md`（第 3/4 刀仍照原图，仅顺序后移）
- M5 嵌入式裁决 A：坞全展开复盘态（双轨分/走势/what-if）在本刀之后另排
- 记忆锚点：[[jianghu-three-scenes-pde]]
