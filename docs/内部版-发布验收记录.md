# 江湖内部版发布验收记录

> 状态：**NO-GO（验收口径已按 ADR-INT-502 简化，下一候选 rc10；等待公司服务器清库重装 + 简化验收）**
> 任务：`INT-502`
> 规则：本文只记录可复核证据；核心旅程冒烟清单、备份恢复真机演练、连续 48–72 小时受控运行和项目所有者拍板未完成前，禁止改为 GO。口径依据 `docs/ADR-INT-502-发布验收简化与清库重装.md`（2026-07-21 项目所有者批准：公司旧库备份留档后清库重装；三旅程→一套冒烟；336h→48–72h；样本 100→20 条，不足且零失败由 Owner 人工判定）。

## 1. 发布身份

| 字段 | 记录 |
|---|---|
| Release ID | `internal-workbuddy-first-rc10` |
| 开始基线 | `a19cbec4b23220eff5e22dc8ffcd9ba537d2a6bd` |
| 候选代码 SHA | `d57f867c7d19e0b4f72a132457c6a10dcdb977d3`（彩排基线 `2a9d9d7` + fast-uri 审计修复，代码零变化仅 lockfile） |
| 部署 ref | `internal-workbuddy-first-rc10`（本记录提交后创建） |
| 回滚 SHA | 部署前由 `scripts/create-release-rollback-point.sh` 自动记录 |
| 分支 | `main` |
| 冻结时间 | `2026-07-22T03:35:04Z` |
| 观察窗口 | 未开始；公司服务器按 `docs/部署-公司内网CentOS7.md` §8A 清库重装并确认健康后开始，连续 48–72 小时（ADR-INT-502） |
| 最终结论 | `NO-GO` |
| rc9→rc10 变更 | ADR-INT-502（验收简化+清库重装）；INT-503 清理（MomentFlow 退役、僵尸组件/孤儿 CSS 删除、文档纠偏、分支 worktree 清理）；INT-504（update.sh 参数化+空库首装自动收尾、fresh-install 集成场景）；releaseMetrics 48h/20 条口径 |

## 2. RC 发布说明

- `CommandRun.idempotencyKey` 改由中央 runner 统一保存 SHA-256；兼容普通命令的存量 raw key 和 person-merge 的存量已哈希 key。
- PostgreSQL migration 在独占锁内失败关闭地迁移存量命令 key；发现异常 person-merge key 或唯一冲突即停止发布。
- App 新实体 ID 使用 `crypto.getRandomValues` 生成 128-bit opaque ID，兼容公司内网 HTTP；服务端停止截断 UUID。
- Person 增加跨库可移植的 `createdAt`，让“观察窗口内新建正式人物的重复合并率”可以直接计算。
- Vite/Vitest 构建测试依赖升级，完整依赖树与生产依赖树均执行 high/critical 门禁。
- CI 补齐 G64111、PostgreSQL schema drift、全包依赖审计和生产镜像构建。
- 增加三套脱敏旅程的自动化证据、受控运行指标提取、发布前回滚点和显式生产回滚脚本。
- 将一次性 bootstrap 的旧模型快照校准到公司库真实的 pre-INT-102 索引布局；bridge 在事务内补齐全部租户索引，并把 `OppRole` / `OpportunityMember` 的二元唯一键转换为租户级唯一键。

## 3. 自动门禁

候选代码 SHA 的远端门禁已在 [GitHub Actions run 29733518359](https://github.com/ZiZ-LG/jianghu/actions/runs/29733518359) 全绿。公司 CentOS 7 的旧 Git 不支持全局 `-C` 选项，`765dc80` 将部署和运维演练统一改为子 shell 切目录执行；`bd37656` 补齐真实 PostgreSQL 演练对兼容辅助函数的装载；`e89e555` 用公司实际 pre-INT-102 索引模型执行完整 PostgreSQL bridge 并通过迁移后零漂移检查。公司服务器正式演练日志仍应保存到只读发布目录并记录 SHA-256。

| 门禁 | 命令 | 结果 | 日志/校验值 |
|---|---|---|---|
| Domain contracts | `cd packages/domain-contracts && npm ci --install-links && npm run typecheck && npm test` | 远端 `PASS` | CI run `29733518359` / job `domain-contracts` |
| App | `cd app && npm ci --install-links && npm run typecheck && npm test && npm run build` | 远端 `PASS`（27 files / 217 tests + build） | CI run `29733518359` / job `app` |
| Server | `cd server && npm ci --install-links && npm run generate && npm run schema:postgres:check && npm run typecheck && npm test` | 远端 `PASS`（37 files / 321 tests） | CI run `29733518359` / job `server` |
| G64111 | `cd packages/g64111 && npm ci && npm run typecheck && npm test` | 远端 `PASS`（2 files / 32 tests） | CI run `29733518359` / job `g64111` |
| PDE kernel | `cd packages/pde-kernel && npm ci && npm run typecheck && npm test` | 远端 `PASS`（3 files / 25 tests） | CI run `29733518359` / job `pde-kernel` |
| 全依赖审计 | 五个包分别执行 `npm audit --audit-level=high` 与 `npm audit --omit=dev --audit-level=high` | 远端 `PASS` | CI run `29733518359` / matrix `dependency-audit` |
| PostgreSQL | `bash scripts/test-postgres-ops-integration.sh` | 远端真实 Docker/PostgreSQL 全链 `PASS` | CI run `29733518359` / job `postgres-operations` |
| 生产镜像 | `docker compose build server web` | 远端 `PASS` | CI run `29733518359` / job `production-images`；公司服务器实际 digest 待填 |

## 4. 核心旅程冒烟清单（ADR-INT-502：取代原三套旅程分别人工验收）

自动回归仍全量保留：旅程 A 位于 `server/tests/workbuddy-e2e.test.ts`；旅程 B/C 位于 `server/tests/internal-release-journeys.test.ts`。人工验收压缩为下表一套冒烟，由两名真实用户 + 一名 viewer 在公司服务器上执行，逐项勾选：

| # | 冒烟项 | 通过标准 | 结果 |
|---|---|---|---|
| 1 | 注册/登录（手机号与邮箱各一） | 进入 Hub，无控制台错误 | ☐ |
| 2 | 新建客户 + 商机（口述与表单各一路） | 落库并出现在 Hub；自算任务入队 | ☐ |
| 3 | 录入情报 → 候选进收件箱 → 人审采纳/驳回 | 采纳前画布无正式边；采纳后建边、驳回不落库 | ☐ |
| 4 | 评分与 PDE 徽章 | 左栏趋赢力与「⬆强攻/▶跟进…」徽章随录入变化 | ☐ |
| 5 | 行动牌完成回填 | done 落库 + Evidence + 审计可查 | ☐ |
| 6 | 同 Idempotency-Key 重试 | 不重复创建实体/行动 | ☐ |
| 7 | 归档 → 恢复 | 数据完整回归 | ☐ |
| 8 | viewer 只读投影 | 只见 primaryOwner 归属客户；写入口全部 403 | ☐ |
| 9 | 跨租户不可见抽查 | 第二租户看不到第一租户任何对象（含按 id 直查） | ☐ |
| 10 | 录音接入（飞书妙记拉取或文件上传任一路） | 转写→抽取成图→候选人审链路通 | ☐ |

| 冒烟执行人/时间 | 证据（SyncRun/CommandRun/AuditEvent ID 抽样） |
|---|---|
| 待填写 | 待填写 |

## 5. 备份恢复与部署回滚

部署前：

```bash
cd /data/jianghu
bash deploy-company-update.sh
```

脚本先在 `/data/jianghu-rollbacks/release-*` 固定：部署前 SHA、server/web image ID 与 tag、认证加密数据库备份。readiness 失败会打印显式回滚命令。回滚会先停写，把备份恢复到随机 `jianghu_restore_rollback_*` 隔离库，切换 `.env`，再恢复旧 SHA 对应的镜像并执行 readiness；工作树保留在当前 `main`，便于下一次修复继续 fast-forward。失败后的数据库不会被覆盖，便于取证。

```bash
bash deploy-company-rollback.sh /data/jianghu-rollbacks/release-TIMESTAMP-ID --confirm
```

| 项目 | 记录 |
|---|---|
| 回滚点目录 | 待填写 |
| 备份认证/checksum | 待填写 |
| 恢复目标库 | 待填写 |
| WorkBuddy smoke | 待填写 |
| 回滚 RTO / RPO | 待演练填写 |
| 旧/新 server、web digest | 待填写 |
| 演练结论 | 未完成，`NO-GO` |

首次公司服务器更新尝试（2026-07-20 UTC）：服务器仍执行 pre-INT501 的 detached `update.sh`，将工作树从 `9e307eb` 快进到 `21cc888` 后，新 Compose 因旧 `.env` 缺少 `OUTBOUND_ALLOWED_HOSTS` 在配置插值阶段失败。该次尝试未进入镜像 build、容器滚动更新或 `migrate deploy`，也未执行 `down`，因此脚本没有改动运行中容器和 `pgdata`。RC2 在 one-time bootstrap 中失败关闭地补齐默认公网白名单和空内网白名单，并在日常更新的任何 Compose 检查前显式预检必填值；恢复步骤见 `docs/部署-公司内网CentOS7.md`。

第二次公司服务器 bootstrap 尝试（2026-07-20 UTC）：环境变量迁移已成功，但 CentOS 7 OpenSSL 不支持 `enc -pbkdf2`，认证备份在生成 marker 前失败；随后新版更新脚本因 marker 缺失而正确拒绝 migration build。RC3 将新备份升级为兼容 OpenSSL 1.0.2+ 的 v3 格式，保留 256-bit 随机 master secret、AES-256-CBC、随机 salt、独立 HMAC 和先验签后解密，并继续支持在具备 PBKDF2 的新版 OpenSSL 上恢复历史 v2 备份。

第三次公司服务器 bootstrap 尝试（2026-07-20 UTC）：RC3 已成功越过备份加密、认证、解密和 `pg_restore`，但通用恢复脚本随后按迁移后 schema 要求 `CommandRun`、`EvidenceEvent` 和 `_prisma_migrations`；pre-INT501 的旧 `db push` 数据库按设计没有 Prisma migration 历史，因此恢复被误判为未就绪，marker 仍未生成，更新脚本继续在 migration build 前失败关闭。RC4 将普通恢复保持在严格 `current` 档位，只允许一次性 bootstrap 对 `jianghu_restore_bootstrap_*` 隔离库使用 `pre-int501` 档位并核验 `Tenant`、`SyncRun`；非 bootstrap 命名的普通隔离恢复选择该宽松档位会被拒绝。

第四次公司服务器 bootstrap 尝试（2026-07-20 UTC）：RC4 仍在隔离恢复 readiness 失败关闭。随后对生产库执行只读 `\dt public.*`，确认 41 张业务表中存在 `Tenant`、`User`、`Account` 和 `EvidenceEvent`，但不存在后期 INT-203 才加入的 `SyncRun`；因此 RC4 的所谓旧库档位仍错误绑定了特定开发阶段。RC5 改为核验自初始 schema 即存在的 `Tenant`、`User`、`Account`，并在备份前记录生产库有序 `public` 表清单签名，恢复后要求隔离库签名完全相同；普通恢复的迁移后严格检查及 bootstrap 目标名前缀限制保持不变。focused 19/19、Server 全量和 typecheck 已通过；真实公司旧库仍须重跑后才能写 marker。

RC5 推送前对抗审查（2026-07-20 UTC）发现 P1：即使 41 表恢复验证通过，旧库仍不等于当前 45 模型，原 entrypoint 只允许“未纳管 schema 与当前模型完全一致”后登记 baseline，直接更新会使新 server 在 migration 前失败并可能进入重启循环。RC6（主体 `6776e93`，重入 `577e7d7`，revision gate `3219a19`，停服中断安全 `2800079`，bridge 恢复与数据门禁 `cbf1fbc`，提交后失败回滚 `fb2cce6`，事务内负责人接管 `f7adc58`）关闭该缺口：固化 2026-07-12 的 41 模型 PostgreSQL schema，只在 live schema 精确匹配该版本或当前模型时登记既有 migration；新增幂等版本化 bridge 补齐 4 张表、列、索引和外键，迁移后必须与当前 datamodel 零漂移。分步 `migrate resolve` 和 bridge 事务中断均可识别续跑；未纳管当前 schema 若含无法区分 raw/digest 的 `CommandRun` 则拒绝自动接管。bootstrap 在写 marker 前用候选镜像对隔离恢复库跑完整 migration，并拒绝脏构建输入；marker 在首次 bridge 成功完成前绑定 Git commit。唯一索引前扫描同步锚和企微绑定冲突；稳定负责人歧义/越租户 ID 检查与唯一姓名回填已纳入 bridge 同一事务，失败会连同 schema 变更整体回滚，自动重启无法绕过。生产更新先构建、停写、迁移再替换；若 migration history 尚未改变，停服区间失败/信号恢复原服务；若 migration 已产生持久变更，则自动执行认证回滚，禁止旧代码连接新 schema。运行版本 SHA 在 pull 前原子持久化，部署成功及显式或自动回滚成功后均同步更新。focused migration/schema/ops 54/54、Server 37 files / 319 tests + typecheck、App 27 files / 217 tests + production build、G64111 32、PDE Kernel 25、双 PostgreSQL schema validate、脚本语法和 diff check 已通过；本轮 P1 修复 focused 34/34 通过，真实 Docker CI/公司服务器 E2E 仍为发布门。

第五次公司服务器 bootstrap 尝试（2026-07-20 UTC）：RC8 已完成认证备份、隔离恢复、恢复库签名核验和候选 server 镜像构建，但在隔离库 migration preflight 发现旧模型不匹配而失败关闭。输出的唯一漂移是 INT-102 引入的租户索引与 `OppRole` / `OpportunityMember` 唯一键变化；与历史提交 `15982ea` 的 schema 精确一致，说明此前 RC6 固化的旧快照晚了一个阶段。失败发生在生产库 migration、marker 写入和运行容器替换之前，因此生产数据库与运行服务未被改动。RC9 将旧快照校准到 `15982ea`，并在既有 bridge 事务内创建全部缺失租户索引、替换两个旧唯一键；GitHub run `29733518359` 已从该旧快照真实创建 PostgreSQL 数据库、执行完整 migration 并通过最终零漂移、恢复与失败注入全链。公司服务器仍须重跑后方可生成 marker。

预推送实现演练（不替代公司服务器最终演练）：2026-07-16 UTC 在隔离 Compose project `jianghu_int502_rollback_drill` 对代码基线 `2cdb90ee10740703200fb2ffec963e4ae1138671` 完成“运行中数据库 → 认证回滚点 → 快照后写入 → 隔离库恢复 → 旧镜像启动 → readiness”全链。恢复后快照前测试记录为 1 条、快照后测试记录为 0 条，输出 `INT502_ROLLBACK_DRILL_OK=1`，临时容器、卷和备份目录均已清理。正式部署仍须记录实际回滚点、镜像 digest、恢复库、RTO/RPO 与 WorkBuddy smoke，故本节仍为 `NO-GO`。

rc10 本机全流程彩排（2026-07-22 UTC，隔离 Compose project `jianghu-rehearsal`，候选 SHA `2a9d9d7`）：按 `docs/部署-公司内网CentOS7.md` §8A/§8B 的命令原样执行——①`git clone` 临时仓+独立 `.env` ②首装 `update.sh`：空库 4 条迁移、三容器 healthy、readiness `{"ok":true}`、7/7 收尾自动写首个认证备份+bootstrap marker ③生产构建浏览器冒烟：注册/登录、载入示例、作战室画布/趋赢力/PDE 徽章/推演坞四列全部正常 ④二次 `update.sh`：existing_db=1 路径（marker 验证→部署前备份→回滚点 `release-20260722T033354Z-*`→no-op 迁移→readiness）全绿 ⑤备份恢复演练：restore 至 `jianghu_restore_drill_*` 隔离库 1.1 秒验证通过，抽查 1 租户/10 人/1 客户与源一致，演练库已删 ⑥`down -v` 清理，团队栈 `jianghu_pgdata` 全程未触碰。另：同日 `scripts/test-postgres-ops-integration.sh` 在本机真实 Docker 全链 PASS（含新增 FRESH_INSTALL 双遍场景）。手册每条命令均被执行过一次；公司服务器正式执行与 RTO/RPO 记录仍待 §8A/§8B。

## 6. 受控运行口径（ADR-INT-502：48–72 小时）

观察对象为一个明确租户，所有查询带 `tenantId`。冻结后每日保存：

```bash
cd /data/jianghu/server
npm run release:metrics -- \
  --tenant TENANT_ID \
  --start 2026-07-XXTXX:XX:XXZ \
  --end   2026-07-XXTXX:XX:XXZ
```

最终检查追加 `--final`，未达到自动阈值时退出非零。

- WorkBuddy 成功率：观察窗口内 `SyncRun` 逻辑幂等命令的 `completed / (completed + failed)`，不少于 20 条终态样本且不低于 99%（不足 20 条且零失败时由项目所有者按 ADR-INT-502 人工判定）。重放不虚增分母。
- 卡住项：`SyncRun.running` 超过 5 分钟或 `CommandRun` lease 过期，必须为 0。
- 重复正式实体率：观察窗口内创建的 `Person` 中，`mergedIntoPersonId IS NOT NULL` 的占比，必须低于 1%。
- 严重数据完整性事故与失败恢复责任人由人工 incident ledger 记录；不能用 AuditEvent 缺失反推“零事故”。
- 生产代码、schema、迁移或指标语义变化后，48 小时窗口重新开始。

| 日期 UTC | 终态命令 | 成功率 | 新建正式人物 | 重复率 | 卡住 | 严重事故 | 失败恢复记录 | 记录人 |
|---|---:|---:|---:|---:|---:|---:|---|---|
| Day 1 | 待填写 |  |  |  |  |  |  |  |
| Day 2 | 待填写 |  |  |  |  |  |  |  |
| Day 3（可选） | 待填写 |  |  |  |  |  |  |  |

## 7. 已知风险与批准

| 风险 | 当前处置 | Owner 决定 |
|---|---|---|
| 已缓存旧前端仍可能提交 legacy ID | 新前端及全部服务端生成改为 128-bit opaque；读写继续兼容存量 ID，避免迁移破坏。已知外租户 ID 被恶意复制仍可能造成全局 PK 冲突，彻底消除需服务端 ID 映射或复合主键。 | 待批准剩余风险或要求扩大改造 |
| 指标采用逻辑命令而非 HTTP attempt | 与幂等语义一致；重放不虚增成功率；服务不可达事故由人工 ledger 补充。 | 待批准 |

## 8. 签署

| 角色 | 姓名 | 时间 UTC | 结论/签名 |
|---|---|---|---|
| 工程 |  |  |  |
| 运维 |  |  |  |
| 旅程 A 验收 |  |  |  |
| 旅程 B 验收 |  |  |  |
| 旅程 C 验收 |  |  |  |
| 项目所有者 |  |  | `GO / NO-GO` |
