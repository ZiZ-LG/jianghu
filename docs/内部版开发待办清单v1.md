# 江湖内部版开发待办清单 v1

**状态：** Active，执行基线已由项目所有者确认  
**日期：** 2026-07-11  
**Owner：** 项目所有者  
**计划基线：** `docs/superpowers/plans/2026-07-11-internal-edition-development.md`  
**架构基线：** `docs/架构-双版本关系与变更治理v1.md`  
**Last verified commit：** `303cf87`

## 使用规则

1. 本文件是内部版唯一日常状态清单；详细做法和验收以计划文档为准。
2. 默认同一时间最多一个任务处于 `IN_PROGRESS`；只有项目所有者明确批准、任务无依赖且不改共享文件时，才可并行两项并记录原因。
3. 任务开始前填入分支/worktree、Owner 和开始日期。
4. 任务只有满足计划中的完成定义并附验证证据后才能勾选。
5. 阶段门未通过，不得开始下一阶段的功能任务。
6. 普通调整更新本清单；重大偏差先按架构基线暂停、评审和批准。
7. 不以“代码已经写了”作为完成标准；没有测试、验收和恢复说明即未完成。

状态值固定为：`PENDING / READY / IN_PROGRESS / BLOCKED / DONE`。

## 当前执行焦点

- 当前阶段：`M5 生产运维和内部发布`。
- 当前状态：`INT-502 最终发布门（按 ADR-INT-502 简化口径）` 为唯一 `IN_PROGRESS`；配套 `INT-503`（无风险清理）/`INT-504`（清库重装投产路径）已完成（见状态更新记录 2026-07-21 行）。
- 下一代码任务：无；INT-502 为当前内部版最终发布门。
- 当前重大偏差：`docs/ADR-INT-502-发布验收简化与清库重装.md`（2026-07-21 项目所有者批准）——公司旧库备份留档后清库重装、验收口径简化（三旅程→一套冒烟、336h→48–72h、样本 100→20）、MomentFlow 退役、机制冗余只立收敛路线不动刀。`docs/ADR-INT-304-行动回填审计缺口.md` 选项 A 已实施并验证。
- 当前阻塞：无。
- 发布前加固：`CommandRun.idempotencyKey` 中央哈希兼容迁移及全局 opaque 实体 ID 契约纳入 `INT-502`；禁止在单一命令端点做破坏存量重放/行动回填兼容性的局部修补。

默认任务顺序：`INT-001 → 101 → 102 → 103 → 104 → 105 → 106 → 107 → 108 → 201 → 202 → 203 → 204 → 301 → 302 → 303 → 304 → 401 → 402 → 403 → 404 → 501 → 502`。每个阶段门在进入下一阶段前单独核验。

## 总清单

### M0：基线与测试地基

- [x] **INT-000｜关系与治理基线、内部计划、状态清单**  
  状态：`DONE`（执行基线已确认）  
  证据：三份文档已生成；业务源码未修改。

- [x] **INT-001｜Fastify App factory、隔离测试库和 CI**
  状态：`DONE`
  Owner：Codex
  日期：2026-07-11
  分支/worktree：`codex/int-001-fastify-test-harness` / `/Volumes/PowerData/.codex-worktrees/jianghu-int-001`
  依赖：INT-000 基线已提交（`2ecf9e3`）
  提交：`this task commit`（`test(server): add isolated Fastify integration harness`）
  验证证据：App `npm run typecheck && npm test && npm run build`（8 files / 85 tests + production build）；Server `npm run typecheck && npm test`（2 files / 7 tests，严格类型检查覆盖 `src/tests/scripts/vitest.config.ts`，缺失 `test.db` 时可自建并同步，`DATABASE_URL=file:./test.db`）；PDE Kernel `npm run typecheck && npm test`（2 files / 24 tests）；`server/prisma/dev.db` mtime 前后不变；Git 未跟踪 `.db`、密钥、生成客户端或构建产物。
  阶段门：通过。

### M1：共享核心、安全和决策正确性

- [x] **INT-101｜共享 Action 契约和 CommandContext**
  状态：`DONE`
  Owner：Codex
  日期：2026-07-12
  分支/worktree：`codex/int-101-domain-contracts` / `/Volumes/PowerData/.codex-worktrees/jianghu-int-101`
  依赖：INT-001  
  提交：`this task commit`（`refactor: share and validate mutation contracts`）
  验证证据：Domain Contracts `npm run typecheck && npm test`（1 file / 11 tests）；App `npm run typecheck && npm test && npm run build`（9 files / 92 tests + production build）；Server `npm run typecheck && npm test`（5 files / 24 tests，含语音逐项信任上下文、server-owned createdBy/_mcpOrigin 与 MCP legacy profile 回归）；PDE Kernel `npm run typecheck && npm test`（2 files / 24 tests）；App、Server 两个生产 Docker 镜像构建成功，镜像中无 `.db`/`.env`；`server/prisma/dev.db` 前后均不存在；`docs/pde-handoff/` 无改动。
  验收锚：前后端只维护一份 Action schema，公开入口无 passthrough/any。

- [x] **INT-102｜父子关系和引用端点租户守卫**
  状态：`DONE`
  Owner：Codex
  日期：2026-07-12
  分支/worktree：`codex/int-102-tenant-parentage-guards` / `/Volumes/PowerData/.codex-worktrees/jianghu-int-102`
  依赖：INT-101  
  提交：`this task commit`（`fix(security): enforce tenant parent-child invariants`）
  验证证据：两租户十类攻击、50 个 existing-tree Action、同租户错树、patched reference、历史 cascade 异常、state 防御过滤、Serializable/P2034、候选采纳/生产/读取 Account 父树与端点隔离、`DELETE_PLAN_ACTION` 反向引用/CAS，以及 undo/redo 持久化顺序与失败恢复回归全部通过（tenant-parentage 28、MCP boundary 7、App store 23）；Domain Contracts 11 tests、App 102 tests + production build、Server 55 tests、PDE Kernel 24 tests 全绿；App、Server 两个生产 Docker 镜像构建成功，PostgreSQL provider 生成通过且镜像中无 `.db`/`.env`；`server/prisma/dev.db` 前后均不存在；`docs/pde-handoff/` 无改动；最终独立审查 `NONE — READY`。
  验收锚：两租户攻击矩阵全部失败，任何树不出现外租户子记录。

- [x] **INT-103｜移除 reset、归档恢复和最小审计**
  状态：`DONE`
  Owner：Codex
  日期：2026-07-12
  分支/worktree：`codex/int-103-archive-recovery` / `/Volumes/PowerData/.codex-worktrees/jianghu-int-103`
  依赖：INT-102  
  提交：`this task commit`（`fix(data): replace destructive reset with audited archive`）
  验证证据：旧 `/api/reset` 返回 404；Account/Opportunity 在线硬删除统一失败；member 可归档但不可恢复/查看归档清单，owner/admin 可恢复完整子树；跨租户 archive/restore 返回通用 404；AuditEvent 与归档写入同事务且只记录字段名，不复制 FORM/日志/纪要；合法归档分支不触发 state 安全误报；Domain Contracts 11 tests、App 103 tests + production build、Server 61 tests、PDE Kernel 24 tests 全绿；App、Server 两个生产 Docker 镜像构建成功，PostgreSQL provider 生成通过；`server/prisma/dev.db` 未产生，`docs/pde-handoff/` 无改动；本地 diff 审查无阻塞项。
  验收锚：日常 API 不可清空租户；客户/商机误操作可恢复。

- [x] **INT-104｜AI/录音/Workbuddy 机器写入失败关闭**
  状态：`DONE`
  Owner：Codex
  日期：2026-07-12
  分支/worktree：`codex/int-104-ingest-trust` / `/Volumes/PowerData/.codex-worktrees/jianghu-int-104`
  依赖：INT-102  
  提交：`this task commit`（`fix(ai): fail closed at machine write boundaries`）
  验证证据：LLM 完整 Zod 输出契约与缺失/异常 `kind+confidence` 失败关闭已覆盖；机器 Action 使用编译期穷举策略，Person/Edge/正式字段绕过被拒绝；Evidence 的 `status/origin` 由服务端上下文强制决定；WorkBuddy 对已有 OppRole/Opportunity/BI/UCV 的修改统一生成 ChangeProposal，数据库唯一 dedupe key 覆盖 pending 草稿且采纳后释放；提案采纳使用事务内 CAS，重复采纳只有一次生效，非法值整体回滚；`human_command` 需服务端 scope + `sourceRef/sourceExcerpt` 才可升信任。Domain Contracts 11 tests、App 103 tests + production build、Server 71 tests、PDE Kernel 24 tests 全绿；Server PostgreSQL 生产镜像构建成功；`server/prisma/dev.db` 未产生，`docs/pde-handoff/` 无改动。
  验收锚：缺 trust 字段不会获得高信任；机器推断只能进入候选/提案。

- [x] **INT-105｜approved Evidence → PDE → 可靠快照**
  状态：`DONE`
  Owner：Codex
  日期：2026-07-12
  分支/worktree：`codex/int-105-pde-evidence` / `/Volumes/PowerData/.codex-worktrees/jianghu-int-105`
  依赖：INT-104  
  提交：`c16d47c`（`spec(pde): define evidence alpha input`）、`5754653`（`feat(pde): apply approved evidence in kernel`）、`this task commit`（`fix(pde): make evidence snapshots reliable`）
  验证证据：Python oracle 先行加入 no-evidence / approved-positive / approved-negative 三案例并生成 schemaVersion 1.1 golden，脚本断言全部旧 params/cases 数值不变，TS 对三案例完整 EvalResult 做 1e-6 递归比对；PDE Kernel `npm run typecheck && npm test`（3 files / 25 tests）；Server `npm run typecheck && npm test`（10 files / 77 tests），集成覆盖 tenant/opportunity/current-pack catalog 隔离、inactive pack 以 tenant-scoped updateMany + scoped read 激活、approved-only 正负分布、审核覆盖值、Evidence IDs + 聚合 alpha + active pack/catalog 版本可回放快照，以及 Serializable 同事务 CAS→计算→快照的提交前不可见与失败全回滚；`server/prisma/dev.db` 未产生。
  验收锚：approved 正/负证据改变对应分布和 pWin；pending/rejected 不改变；旧 golden 不漂移。

- [x] **INT-106｜生产密钥、出站 URL 与依赖收口**
  状态：`DONE`
  Owner：Codex
  日期：2026-07-12
  分支/worktree：`codex/int-106-security-egress` / `/Volumes/PowerData/.codex-worktrees/jianghu-int-106`
  依赖：INT-001
  提交：`this task commit`（`fix(security): fail closed on secrets and outbound requests`）
  验证证据：TDD RED 先证实任意 AI baseUrl 可触发 fetch、私网/IPv6/DNS 混合结果/重定向/响应上限/生产密钥均未失败关闭；独立审查补充 RED 证实 Compose 未传白名单、NAT64/IPv4-compatible/translated/6to4 可绕过及缺少真实网络回归。最终 focused `npm test -- outbound-url outbound-undici compose-security production-config`（4 files / 48 tests）、Server `npm test && npm run typecheck`（14 files / 125 tests）全绿；集中出站边界覆盖 AI、QCC、Getnote、飞书和企微，所有直接 fetch 收敛到手动重定向、逐跳 allowlist + 全 A/AAAA/IPv4 转换形式校验、DNS 结果固定 dispatcher、AbortSignal timeout 和成功/错误/SSE 响应上限；真实 Undici 本地集成覆盖 DNS 固定连接、header/body stall、流式超限取消及 302/303/307/308 语义；`docker compose config` 证实两项白名单传入 server，`docker compose build server` 成功；`npm audit --omit=dev` 为 0 vulnerabilities；镜像无 `.db`/`.env`，缺 JWT_SECRET/AI_KEY_SECRET 实际启动在监听前退出；本地安全审查修复跨主机重定向凭据/请求体泄漏、DNS 重绑定窗口和超限响应未取消问题。
  验收锚：生产密钥 fail closed；SSRF 矩阵通过；依赖无 critical/high 或有逐项不可达证明。

- [x] **INT-107｜稳定用户归属、敏感 ACL 和企微回调权限**
  状态：`DONE`
  Owner：Codex
  日期：2026-07-12
  分支/worktree：`codex/int-107-stable-ownership` / `/Volumes/PowerData/.codex-worktrees/jianghu-int-107`
  依赖：INT-102
  提交：`this task commit`（`fix(auth): enforce stable ownership and sensitive ACLs`）
  验证证据：TDD RED 首先证明稳定 owner backfill 和 tenant+corp token cache 不存在；独立审查又补充了在线 owner 合同、PDE 侧信道、voice logs 提案、企微竞态、OAuth 重放、bind 冲突、推卡泄漏和坏 visibility 等 RED；复审进一步覆盖 legacy sensitive 布尔映射与 OAuth 登录绑定 CSRF。最终 focused `npm test -- visibility-acl wecom-authorization`（2 files / 15 tests）、Server `npm test && npm run typecheck`（16 files / 141 tests）、Domain Contracts（12 tests）、App（103 tests）、PDE Kernel（25 tests）全绿。Account Web/MCP/WorkBuddy 合同显式传 `primaryOwnerUserId`，服务端校验租户内稳定 ID，姓名仅展示；幂等 backfill 仅自动链接租户内唯一精确同名用户，重名/未匹配/缺失/无效稳定 ID 进人工清单。State 强制传 principal，对 viewer 归属、private BI 以及 self/team/org 动态执行 ACL；legacy 无 visibility 时仅接受布尔 sensitive（true→team，false/缺失→org），畸形类型 fail closed。PDE assemble 同步过滤 private BI/依赖 UCV，viewer 不读旧快照。Person log 提案收紧为单条 append，旧 pending logs 仅在严格头部 append 形态兼容，`createdBy` 始终服务端注入。企微 token 按 `tenantId+corpId` 隔离，accept/reject 在同一 Serializable 事务内复核唯一 bind/User/role 并 CAS；OAuth provider state 与仅返回原已认证 app 的 requestId 分离，回调只保存 10 分钟 pending，原 tenant/user 会话须显式 confirm 且实时复核角色后才原子绑定/消费，诱导扫码、错误登录、重放和过期均失败关闭；bind 增加 `(tenantId,wecomUserid)` 唯一约束及可重跑冲突报告，推卡前过滤 deleted/viewer/重复绑定。
  验收锚：同名 viewer 不串权；private/self 数据由服务端隔离；失效用户不能企微审批。

- [x] **INT-108｜单一共享 G64111 包**
  状态：`DONE`
  Owner：Codex
  日期：2026-07-12
  分支/worktree：`codex/int-108-shared-g64111` / `/Volumes/PowerData/.codex-worktrees/jianghu-int-108`
  依赖：INT-101  
  提交：`this task commit`（`refactor(g64111): use one shared scoring package`）
  验证证据：TDD RED 首先用同一 fixture 捕获 App/Server `ScoreBreakdown` 的 `bandLabel/strategy` 形状漂移，并证明人为 1 分分歧会失败。最终 `packages/g64111/` 是唯一公式、常量、项目元数据和逐人贡献实现，App/Server 仅保留 typed adapter/re-export；Server 策略路由也直接消费共享项目元数据，不再保留副本。App 和 Server 各自独立对同一个静态 compatibility fixture 做完整精确断言，Server 测试不再跨目录导入 App。可移植 fixture 覆盖缺角色、多 A/D 取低、unknown/confidence gate、C1 家庭 7 问与小数采购状态、C2/C6 BI+UCV、P2、负分、75/50/25 默认阈值和自定义 profile。G64111 `npm run typecheck && npm test` 2 files / 27 tests，Server parity 10 tests，App 9 files / 96 tests + production build，Server 17 files / 151 tests，Domain Contracts 12 tests，PDE Kernel 25 tests 全绿；G64111 与 Server runtime audit 均为 0 vulnerabilities。根目录与 App/Server 项目配置固定 `install-links=true`，从干净 `git archive` 中仅用普通 `npm ci` 完成 App 全量门禁和 Server 独立 generate/typecheck/full test。App/Server 生产 Docker 镜像均成功，镜像内 `@jianghu/g64111` 可解析，且修复 `pde-kernel` 发布白名单遗漏运行期 `fixtures/seeds` 导致 PDE 路由 500 的打包问题；镜像无 `.db`/`.env`，`docs/pde-handoff/` 未改动。真实 WorkBuddy `score.py` 执行器通过 8/9，小数 C1 用例被外部脚本拒绝；该事实作为 M1 外部阻塞记录，没有被误报为通过。
  验收锚：App、Server、MCP 对同一 fixture 逐项与总分完全一致。

#### M1 阶段门

- [x] Server 集成测试覆盖租户、RBAC、机器写入、PDE 证据链。
- [x] `/api/reset` 和 member 级联硬删除不可达。
- [x] 不存在客户端伪造 origin/status 升级信任的路径。
- [x] G64111 和 PDE 各自只有一个权威实现。
- [x] M1 全量类型检查、测试和构建通过。
- [x] WorkBuddy 源码与装配产物中的 `score.py` 完整通过共享 compatibility fixture。

阶段门：`PASSED`。WorkBuddy 外部提交 `241e4a6` 修复小数 C1 输入；源码、装配包共享脚本和云盘骨架三份 `score.py` 字节一致，SHA-256 均为 `6c54d609cae6ffa7df365709936fc2f42e94996ad7e0480cfe456d4b6a0f7b76`，逐份运行共享 compatibility fixture 均为 9/9。`INT-201` 解锁为 `READY`，未在本阶段门更新中提前实施。

### M2：数据可靠性与原子写入

- [x] **INT-201｜前端写入队列、草稿提交和保存状态**
  状态：`DONE`
  Owner：Codex
  日期：2026-07-14
  分支/worktree：`codex/int-201-sync-coordinator` / `/Volumes/PowerData/.codex-worktrees/jianghu-int-201`
  依赖：INT-101  
  验收锚：快速输入、弱网、409 和重试后，服务端最终值等于用户最后值。
  证据：同实体 Action 穷举串行、不同实体并行、400ms 草稿/80ms 拖拽合并、失败保留与安全重试、409 rebase 后排队版本级联、统一 401、会话恢复/重置和可见保存状态均有回归；App 127 tests + typecheck + build、G64111 27 tests + typecheck、Server typecheck 全绿。

- [x] **INT-202｜复合动作事务化和幂等 CommandRun**
  状态：`DONE`
  Owner：Codex
  日期：2026-07-14
  分支/worktree：`codex/int-202-command-runs` / `/Volumes/PowerData/.codex-worktrees/jianghu-int-202`
  依赖：INT-102、INT-201  
  验收锚：骨架、voice、行动反馈和批量审核故障注入无部分成功。
  证据：CommandRun 按 tenant/actor/kind/key 唯一并绑定请求哈希，Serializable/锁竞争有界重试与可重放；voice/recording 在外部模型调用前占用可恢复租约，事务内完成落库与派生 Job；行动结果使用业务级 CAS 和确定性 Evidence ID；商机骨架、五类收件箱批审均为单命令，PDE 快照同事务写入。覆盖故障回滚、并发重放、失败后同 key 重试、租约恢复、跨租户/只读拒绝、敏感摘要脱敏。Server 166 tests、App 128 tests + build、G64111 27 tests 与各端 typecheck 全绿。

- [x] **INT-203｜Workbuddy 唯一约束、sync bundle 和 SyncReceipt**
  状态：`DONE`
  Owner：Codex
  日期：2026-07-14
  分支/worktree：`codex/int-203-workbuddy-sync` / `/Volumes/PowerData/.codex-worktrees/jianghu-int-203`
  依赖：INT-104、INT-202  
  验收锚：并发重放只产生一套数据；每次同步可解释 created/updated/proposed/skipped/failed。
  提交：`this task commit`（`feat(mcp): add atomic workbuddy sync receipts`）
  证据：租户内 Account/Opportunity/VisitNote 业务锚由数据库复合唯一约束保护，部署前逐表扫描空值/重复并在全新空库安全放行；`sync_intel_bundle` 在 Serializable 单事务内写正式事实、pending 候选、ChangeProposal、任务和隐私安全 SyncRun，失败回滚后留下可重试 failed 回执，同 key 并发/跨 actor 重放只产生一套数据；旧离散工具复用同一 service 并保持一版兼容。Server 192 tests、App 128 tests + production build、G64111 27 tests、PDE Kernel 25 tests 及各端 typecheck 全绿；空库冲突预检实测 `conflictCount: 0`，最终独立复审问题已关闭。

- [x] **INT-204｜后台任务租约、重试和崩溃恢复**
  状态：`DONE`
  Owner：Codex
  日期：2026-07-14
  分支/worktree：`codex/int-204-job-recovery` / `/Volumes/PowerData/.codex-worktrees/jianghu-int-204`
  依赖：INT-102  
  验收锚：worker 崩溃后任务自动恢复，多 worker 不重复消费。
  提交：`this task commit`（`fix(jobs): add leases retries and crash recovery`）
  证据：四类后台任务使用租户内稳定去重键和 Serializable 入队事务，50 条在途配额在不同 key 并发下仍由数据库串行保证；claim 以 `leaseOwner + leaseToken + leaseUntil` 条件 CAS，5 分钟 lease 每 1/3 周期续租，过期任务按 1s/2s 指数退避并在 3 次后进入 failed；旧 `processing + leaseUntil=null` 行可恢复，旧 schema 含 processing 任务升级实测保留 `attempts=2`。PersonSuggestion/Note/RelSuggestion/Transcript 的持久化均经 token fence，人物候选写入与 Job done/result 同一事务，故障注入验证一起回滚。Server 200 tests、App 128 tests + production build、G64111 27 tests、PDE Kernel 25 tests 与各端 typecheck 全绿；最终独立复审 `CLEAN`。

#### M2 阶段门

- [x] 同实体写入不乱序，不再每个按键直接形成并发请求。
- [x] 所有复合业务动作有事务和 idempotency key。
- [x] Workbuddy 重复/并发同步没有重复正式实体或重复 pending。
- [x] Job 具备 lease、退避、失败终态和恢复测试。

### M3：内部最低纠错和 Workbuddy 适配

- [x] **INT-301｜关键字段纠错、归档恢复和重新绑定**
  状态：`DONE`
  Owner：Codex
  日期：2026-07-14
  分支/worktree：`codex/int-301-repair-tools` / `/Volumes/PowerData/.codex-worktrees/jianghu-int-301`
  依赖：INT-103、INT-201、INT-203  
  提交：`this task commit`（`feat(internal): add minimum viable data repair tools`）
  验证证据：TDD RED 先证实修正/重绑端点缺失、关键字段白名单不可达、稳定负责人归属缺失、legacy 负责人会被误清、客户/商机并发覆盖、dirty draft 可绕过版本冲突、重复重试会制造审计、SyncRun 50 条截断/冒号误归因、日期与 RBAC 缺口；最终 Server focused 14 tests、Server 214 tests、App 137 tests + production build、G64111 27 tests、PDE Kernel 25 tests 全绿，两轮质量/安全复审均为 CLEAN；`server/prisma/dev.db` 未产生，`docs/pde-handoff/` 无改动。
  验收锚：挂错客户/商机的记录可由业务用户修正，无需改数据库。

- [x] **INT-302｜重复人物安全合并**
  状态：`DONE`
  Owner：Codex
  日期：2026-07-14
  分支/worktree：`codex/int-302-person-merge` / `/Volumes/PowerData/.codex-worktrees/jianghu-int-302`
  依赖：INT-301  
  提交：`this task commit`（`feat(internal): merge duplicate stakeholders safely`）
  验证证据：TDD RED 覆盖合并端点/RBAC、归档读取、归档商机冲突、双人物 CAS、故障回滚、FORM/日志语义合并、全引用迁移、边自环/重复、提案与提醒父树、Advisor 归档回写、稳定幂等键、错误脱敏，以及 Edge/RelSuggestion missing/cross-account/archived 端点写前失败关闭；最终 Server focused 13 tests、Server 227 tests、App 140 tests + production build、G64111 27 tests、PDE Kernel 25 tests 全绿，Server PostgreSQL 生产镜像构建成功，最终独立质量/安全复审 `CLEAN / READY`；`server/prisma/dev.db` 未产生，`docs/pde-handoff/` 无改动。
  验收锚：引用全部迁移，无悬空数据、自环重复边或重复 OppRole。

- [x] **INT-303｜MCP Token scope 和预设**
  状态：`DONE`
  Owner：Codex
  日期：2026-07-14
  分支/worktree：`codex/int-303-token-scopes` / `/Volumes/PowerData/.codex-worktrees/jianghu-int-303`
  依赖：INT-104、INT-203  
  提交：`this task commit`（`feat(mcp): scope workbuddy access tokens`）
  验证证据：TDD RED 先证实只读/调研 Token 缺少最小权限边界及 viewer 角色变化仍整段拒绝；最终 12 项 focused HTTP 回归覆盖只读与调研越权写、无 Account/候选/SyncRun/CommandRun/AuditEvent 副作用、吊销、成员移除、当前角色降级、原始 scope 伪造、畸形/未知 scope、未知 tokenVersion、legacy v0 安全只读并提示重发、动态 bundle 组合 scope、旧 JWT 当前角色映射，以及 viewer 本人客户投影、直接 ID 脱敏拒绝、候选隔离和 private BI/UCV 评分侧信道。Server 239 tests、App 141 tests + production build、G64111 27 tests、PDE Kernel 25 tests 与各端 typecheck 全绿；SQLite 与 PostgreSQL provider Prisma 生成/校验通过；两轮独立规格/质量/租户安全复审修复 viewer 跨负责人读取与 private BI/UCV 分数泄漏后均为 `CLEAN`；`server/prisma/dev.db` 未产生，`docs/pde-handoff/` 无改动。
  验收锚：read/propose/sync 权限不能互相越界，吊销和成员移除立即生效。

- [x] **INT-304｜Workbuddy → 人审 → PDE → 行动契约 E2E**
  状态：`DONE`
  Owner：Codex
  日期：2026-07-14
  分支/worktree：`codex/int-304-workbuddy-e2e` / `/Volumes/PowerData/.codex-worktrees/jianghu-int-304`
  依赖：INT-105、INT-203、INT-303  
  提交：`this task commit`（`test(mcp): cover workbuddy to decision-loop journey`）
  验证证据：虚构 fixture 真实 HTTP 两阶段旅程覆盖 WorkBuddy 同步/重放、候选人审、Evidence→PDE 快照、行动 `up/down/flat` 回填、审计后故障回滚、同/不同幂等键并发、WorkBuddy Token 与 viewer 拒绝、跨租户无副作用；最终 Server focused 20 tests、Server 244 tests、App 141 tests + production build、G64111 27 tests、PDE Kernel 25 tests 全绿，各端 typecheck、SQLite Prisma validate/generate 与 PostgreSQL 生产镜像构建通过。独立测试/维护性/安全/性能/API/红队复审确认原子性、重放唯一性、RBAC 和租户隔离无阻断；两项既有中央数据最小化契约缺口登记至 INT-502 发布前加固。
  验收锚：虚构 fixture 全链通过，重复运行不增加重复实体。

#### M3 阶段门

- [x] 内部用户可完成改、撤、归档、恢复、重绑、合并和溯源查询。
- [x] Workbuddy Token 遵守最小权限。
- [x] 完整 E2E 自动化通过。

阶段门：`PASSED`。INT-301～INT-304 全部完成并独立验证；INT-401 解锁为 `READY`，未在本阶段门更新中提前实施。

### M4：决策主链与内部体验收口

- [x] **INT-401｜ADURC、主 D、P4 和 G64111 界面语义**
  状态：`DONE`
  Owner：Codex
  日期：2026-07-14
  分支/worktree：`codex/int-401-g64111-semantics` / `/Volumes/PowerData/.codex-worktrees/jianghu-int-401`
  依赖：INT-108、INT-202  
  验收锚：无 TB/空 C/错误多 D 文案；主 D、P4 由服务端强制正确。
  验证证据：G64111 32 tests、domain-contracts 13 tests、Server 265 tests、App 161 tests + production build 全绿；SQLite Prisma validate/generate/db push 与 PostgreSQL 生产镜像生成/构建通过。

- [x] **INT-402｜MomentFlow、业务日期和审核上下文**
  状态：`DONE`
  Owner：Codex
  日期：2026-07-15
  分支/worktree：`codex/int-402-moment-flow` / `/Volumes/PowerData/.codex-worktrees/jianghu-int-402`
  依赖：INT-201、INT-202  
  验收锚：北京时间边界、逾期行动、Evidence-only 和拜访上下文测试通过。
  验证证据：App 整分支最终全量 25 files / 197 tests、TypeScript typecheck 与 production build 全绿；Server 整分支 focused 4 files / 35 tests（含 `TZ=America/Los_Angeles`）、全量 33 files / 281 tests 与 typecheck 全绿。服务端共享 `Asia/Shanghai` 业务日 helper 覆盖 voice 纪要/日志/Evidence、单审/批审 `reviewedAt`、巡检逾期与 `doneAt` 北京 00:30 边界，巡检对存量非法 start/end date 失败关闭而不生成 `NaN` 提醒；MomentFlow 按逾期、`endDate||startDate`、startDate/accountId/id 跨客户稳定排序；voice 父实体预检后竞态归档返回 scoped 404 且精确取消自己的 reservation，不删 completed/他人/新租约，无 CommandRun/VisitNote/Evidence 副作用；logout 清理全部 tenant-scoped inbox/拜访/批审临时态，所有 `inboxList` 刷新均由 generation+token 守卫。API request 用单一规范化 `Headers` 实例保留显式 Authorization、仅在缺失时补全局 bearer，并从同一实例捕获实际 Bearer；401 只在该身份仍是当前 token 时广播 unauthorized，旧会话/旧匿名/显式 Basic 或空 Authorization 的 401 不能登出新会话。`app/src+server/src` 已无 `toISOString().slice(0,10)`；剩余 full ISO 均为 createdAt/审计/溯源/巡检心跳协议时间戳。未改 Prisma schema、共享 Action 契约、G64111、PDE kernel 或 PDE 交接包。

- [x] **INT-403｜AI 上下文最小化和内部帮助**
  状态：`DONE`
  Owner：Codex
  日期：2026-07-15
  分支/worktree：`codex/int-403-ai-context-privacy` / `/Volumes/PowerData/.codex-worktrees/jianghu-int-403`
  依赖：INT-107、INT-303  
  验收锚：无权内容不进入模型上下文；帮助只描述真实流程。
  验证证据：TDD RED 先证实 memberScoped 外部人物、原始日志与客户端伪造 context 会进入旧路径，且无 `ContextManifest`；最终复审又证明同形 manifest 的 token 未绑定 tenant/actor/opportunity/options，异步模型响应也可跨商机、焦点人或选项回写新界面。最终 `/api/ai/simulate` 与 strategy suggest/actions/prefill/milestone-actions 五个模型入口统一由服务端按 tenant+actor+opportunity 重建上下文，客户端只传范围、排除选项与受限用户输入；private BI、self 日志、memberScoped 范围外人物/关系/依赖 UCV 始终排除，原始日志和 FORM 默认关闭且只能由用户显式开启。manifest token 用稳定序列化绑定 tenant、actor、opportunity、标准化 options 与无正文 manifest，跨 actor 重放返回 409；foreign tenant 在 preflight/五执行入口均 scoped 404 且不泄密，viewer 均 403。Advisor 和 DeliberationDock 的所有模型触发在调用前展示实际排除类别与日志数量，且只有 preflight 可更新 UI manifest；五类异步执行结果、错误、busy、候选采纳和 draft dispatch 均按 account/opportunity/person/token/options/generation 校验，切范围会清空临时候选与状态，旧响应不得显示、留档或落到新商机。Dock 的抽屉预填、策略卡派发、里程碑排行动另以 target id、person、相关输入指纹与 nonce 绑定当前操作；切抽屉、编辑人物/内容、删除目标均立即使旧响应失效，旧 finally 不能清除新请求 busy。Advisor assistant 分析还绑定焦点人 scope，恢复无标记历史、切人或切 raw logs/FORM 均不回注，用户本人最新输入仍保留。内嵌/完整手册只保留 Workbuddy、MCP Token、收件箱/人审、纠错、合并、恢复和同步失败判断真实流程。App focused 16 tests、全量 25 files / 209 tests、typecheck/build；Server focused 10 tests、全量 33 files / 286 tests、typecheck；G64111 32 tests 全绿；范围/敏感正文/密钥/lockfile 扫描与 `git diff --check` 通过，未改 schema、Action、G64111、PDE 或依赖。

- [x] **INT-404｜认证错误、Modal、横屏遮罩和键盘操作**
  状态：`DONE`
  Owner：Codex
  日期：2026-07-15
  分支/worktree：`codex/int-404-a11y-auth` / `/Volumes/PowerData/.codex-worktrees/jianghu-int-404`
  依赖：INT-201  
  验收锚：黑盒确认的 4 个问题关闭，核心弹层可由键盘和读屏操作。
  验证证据：TDD RED 先证实认证校验模块缺失；最终注册/登录在请求前给出中文必填、手机号/邮箱和密码错误，输入具备 required/name/autocomplete/minLength 与 live alert 语义，未知英文/网络错误收口为中文。Modal 具备 dialog/aria-modal/label、初始焦点、Tab/Shift+Tab 环、Escape 和直接按钮/下拉菜单触发点焦点归还；竖屏引导显示时同级工作台全部 inert+aria-hidden，仅两个操作进入可访问树，关闭后完整恢复。客户卡、趋赢力条目、作战档案导航及沙盘策略/里程碑/终局/行动均可键盘激活。真实浏览器在 320/375/768/1440 宽度无文档横向溢出；App 26 files / 216 tests、typecheck/build，Server typecheck，G64111 32 tests 全绿。

#### M4 阶段门

- [x] 桌面核心旅程和手机时刻流均无已知高/中严重断点。
- [x] G64111 术语、选择约束、服务端计算和 UI 解释一致。
- [x] AI 数据范围可见、可裁剪、可审计。
- [x] 当前帮助与真实页面一致。

### M5：生产运维和内部发布

- [x] **INT-501｜版本化 PostgreSQL migration、备份与恢复**
  状态：`DONE`
  依赖：M1、M2 阶段门通过  
  验收锚：部署不再 db push；空库/备份恢复演练成功；readiness 检查数据库。
  验证证据：SQLite 单一模型源机械渲染 PostgreSQL schema，baseline migration 与漂移测试纳管；容器先执行同步锚冲突扫描，再对空库 `migrate deploy`，历史 `db push` schema 仅在 live diff 完全一致时登记 baseline。liveness 不查库，readiness/兼容 health 查库且 503/超时失败关闭。备份使用独立非周期 64-hex master secret、域分离加密/MAC 密钥与 metadata+密文 HMAC；PID+进程启动身份+nonce owner、串行 guard、stale quarantine 和 owner compare 防 ABA，operation guard 绝不自动回收、超时失败关闭，主锁只在持 guard 后重读并回收。周期检测覆盖 64 的全部真因子 1/2/4/8/16/32。Compose 容器/config/卷检测与数据库存在性都为 present/absent/error 三态，只有成功空查询可证明 absent；清理查询失败返回 critical 70。bootstrap trap 覆盖 restore/smoke/drop/query 全路径，marker 同文件系统原子发布并绑定 Compose project、生产库和真实认证备份，update 在 pull/build 前重验。隔离 PostgreSQL 演练通过故意 smoke 失败清理、query error critical、pre-INT501 bootstrap、双并发备份、恢复/replace、WorkBuddy 数据、错 key/篡改/坏归档与资源清理。Server 35 files / 309 tests、App 26 files / 216 tests + build、G64111 32 tests、PDE Kernel 25 tests、双 Prisma validate、Docker production build、脚本语法、漂移/敏感/PDE 范围扫描全绿。

- [ ] **INT-502｜最终发布门（ADR-INT-502 简化口径）**  
  状态：`IN_PROGRESS`
  依赖：全部任务  
  验收锚（2026-07-21 按 ADR-INT-502 改写）：自动门禁全绿；公司服务器完成「旧库备份双份留档 → 清库 → fresh install」；核心旅程冒烟清单逐项通过；备份恢复+回滚真机演练完成；48–72 小时受控运行指标达标（样本 ≥20 条或 Owner 人工判定）；项目所有者拍板 GO。发布前加固（CommandRun key 哈希迁移、opaque ID）已随 RC 完成。

#### M5 发布门

- [x] 全项目 TypeScript、测试和生产构建通过。
- [x] 无跨租户读写或父子注入。
- [x] 无机器内容绕过人审。
- [x] 无复合操作部分成功。
- [x] 无 critical/high 依赖漏洞，或有项目所有者批准的逐项不可达证明。
- [ ] 最近备份可恢复，部署可回滚。
- [ ] Workbuddy 命令成功率在 48–72 小时受控窗口内不低于 99%（样本 ≥20 条或 Owner 人工判定，ADR-INT-502），重复正式实体率低于 1%。
- [ ] 项目所有者拍板 GO（签署表留档，ADR-INT-502）。

## 预计节奏与容量边界

按一名全职开发、严格小步验证估算：

| 阶段 | 预计 | 容量重点 |
|---|---:|---|
| M0 | 2–3 天 | 测试地基 |
| M1 | 3–4 周 | 共享核心、安全、PDE/G64111 |
| M2 | 2–3 周 | 写入、事务、Workbuddy 同步 |
| M3 | 1.5–2 周 | 最低纠错和契约回归 |
| M4 | 1–1.5 周 | 主旅程和体验收口 |
| M5 | 1 周开发 + 2 周受控观察 | 迁移、恢复、发布认证 |

估算用于容量安排，不作为降低验收标准的理由。两名开发可并行无共享文件的任务，但 M1/M2 的阶段依赖仍不可跳过。

纯内部专属需求最多占同期研发容量的 15%–20%；共享核心、安全和可靠性保持 50%–60%；其余容量用于已经批准的商业版独立计划，不在本清单中登记。

## 偏差与阻塞记录

当前没有重大偏差或未解决阻塞。下表保留阻塞发生与解除的完整历史；后续记录使用以下字段，每条一行：

| 日期 | 任务 | 类型 | 事实证据 | 影响 | 处理决定 | 批准人 |
|---|---|---|---|---|---|---|
| 2026-07-12 | INT-108 / M1 Gate | 外部阻塞 | WorkBuddy 源码与两份装配产物的 `score.py` SHA-256 均为 `bbe405a06c3c6d26717f3656b2bdf584a38307541935755aa7ddb14525eb84fd`；真实执行兼容 fixture 仅 8/9，`fractional-c1-procurement-third` 报 C1 不是整数 | WorkBuddy 离线分可与江湖权威分漂移，M1 阶段门不能通过 | 离线展示继续禁用；外部修复源码并重新装配两份产物，逐份重跑 9/9 后再解锁 INT-201 | 待项目所有者确认 |
| 2026-07-12 | INT-108 / M1 Gate | 阻塞解除 | WorkBuddy 外部提交 `241e4a6`；三份 `score.py` SHA-256 均为 `6c54d609cae6ffa7df365709936fc2f42e94996ad7e0480cfe456d4b6a0f7b76`；聚焦回归 4/4、事实转换 21/21，逐份共享 fixture 9/9 | 离线评分重新与江湖权威分契约一致 | M1 阶段门转为 PASSED，INT-201 解锁为 READY | 项目所有者已授权外部修复 |

只有实际发生偏差或阻塞时才增加记录，不预建空白结论。

## 状态更新记录

| 日期 | 变更 |
|---|---|
| 2026-07-11 | 建立 v1 基线；INT-000 文档产出完成；等待项目所有者确认后从 INT-001 开始。 |
| 2026-07-11 | 项目所有者确认执行基线；INT-001 转为 READY；下一对话先提交基线文档，再启动测试地基。 |
| 2026-07-11 | 四份执行基线已提交为 `2ecf9e3`；INT-001 转为 IN_PROGRESS，使用专用分支/worktree 执行。 |
| 2026-07-11 | INT-001 完成：Fastify App factory、隔离 SQLite/Prisma 测试上下文和三端 CI 已建立；App/Server/PDE 全量检查通过，`dev.db` 未创建；M0 阶段门通过，INT-101 为下一任务但尚未启动。 |
| 2026-07-12 | INT-101 转为 IN_PROGRESS；使用 `codex/int-101-domain-contracts` 与专用 worktree，按共享 wire Action / 本地 StoreAction 分离及服务端 CommandContext 口径执行。 |
| 2026-07-12 | INT-101 完成：51 个 mutation commands 收敛为共享严格契约，公开入口统一校验服务端 CommandContext；App/Server/PDE、生产构建及两个 Docker 镜像全部通过，INT-102 为下一任务但尚未启动。 |
| 2026-07-12 | INT-102 转为 IN_PROGRESS；使用 `codex/int-102-tenant-parentage-guards` 与专用 worktree，按事务内 scope guard、复合索引基础和 state 防御过滤三层保护执行。 |
| 2026-07-12 | INT-102 完成：50 个 existing-tree Action 已统一执行租户/父子/引用守卫，父对象删除对外租户 cascade 异常失败关闭，顶层动作使用 Serializable + bounded P2034 retry；state 历史异常过滤、复合索引基础、全量测试、PostgreSQL Docker 门禁及独立审查全部通过，INT-103 尚未启动。 |
| 2026-07-12 | INT-102 whole-branch review 发现候选人物/关系采纳仍可绕过正式 mutation scope guard；任务恢复为唯一 IN_PROGRESS，先补历史 malformed candidate RED，再在既有事务内失败关闭。 |
| 2026-07-12 | INT-102 补充修复完成：候选人物/关系采纳在同一事务内校验 tenant/account/opportunity/person 父树并先做 tenant-scoped pending CAS；错树、竞争失败和后续端点失败统一回滚 override/Person/Edge/角色/成员，跨 account 历史脏候选保持 pending 不改写；全量门禁与二次独立审查通过，INT-103 尚未启动。 |
| 2026-07-12 | INT-102 final whole-branch review 发现两个 Important：候选 producer/read 仍缺 Account 父树隔离，删除 PlanAction 未同步清理/恢复 StrategyCard 反向引用；任务再次恢复为唯一 IN_PROGRESS，合并执行一轮 TDD 修复。已知 PostCommitEffect focused test gap 仅记录为非阻塞 Minor。 |
| 2026-07-12 | INT-102 最终补充修复完成：候选 producer/read 按 Opportunity Account 校验正式/候选端点并隐藏历史脏行；删除 PlanAction 在同一事务内只清合法同树卡片且 CAS 失败回滚；前端反向引用与 undo/redo 使用整批持久化队列、exact history claim 和失败刷新屏障。全量 192 tests、双生产镜像与最终独立复审 `NONE — READY`；PostCommitEffect focused test gap 保留为非阻塞 Minor，INT-103 尚未启动。 |
| 2026-07-12 | INT-102 已推送至 `origin/main`；INT-103 转为唯一 IN_PROGRESS，使用 `codex/int-103-archive-recovery` 与专用 worktree，按移除 reset、Account/Opportunity 可恢复归档、最小 AuditEvent 和前端归档入口执行。 |
| 2026-07-12 | INT-103 完成：移除 `/api/reset`，禁用 Account/Opportunity 在线硬删除，以 tenant-scoped 归档/owner-admin 恢复和最小 AuditEvent 替代；前端客户卡改为可恢复归档并提供管理员归档管理；全量 199 tests、双生产镜像与本地安全审查通过，INT-104 尚未启动。 |
| 2026-07-12 | INT-103 已推送至 `origin/main`；INT-104 转为唯一 IN_PROGRESS，使用 `codex/int-104-ingest-trust` 与专用 worktree，按缺失 trust 字段失败关闭、服务端决定来源/状态、机器 Evidence 待审和正式字段机器修改转 ChangeProposal 执行。 |
| 2026-07-12 | INT-104 完成：LLM 输出经完整 Zod 校验且缺失/异常 trust 字段失败关闭，机器 Action 穷举策略阻止正式写绕过，Evidence 来源/状态由服务端决定，已有正式字段机器修改转可稳定去重的 ChangeProposal，采纳用事务 CAS 并覆盖并发/回滚；全量 209 tests、前端生产构建与 Server PostgreSQL 生产镜像通过，INT-105 尚未启动。 |
| 2026-07-12 | INT-105 完成：版本化 PDE oracle 定义可选 `evidence_alpha` 并保持旧 golden 全值不漂移；服务端只按当前 active pack 的 tenant-scoped SignalCatalog 聚合 approved Evidence，inactive pack 激活写/读同样 tenant-scoped，快照完整保存 Evidence IDs、三态增量和 pack/catalog 版本；审核 CAS、PDE 计算与快照 insert 在同一 Serializable 事务内完成，失败整体回滚且提交前外部不可见。PDE Kernel 25 tests、Server 77 tests 与两端严格类型检查全绿，`server/prisma/dev.db` 未产生；INT-106 尚未启动。 |
| 2026-07-12 | INT-108 完成：G64111 公式、741 元数据、ScoringProfile 和逐人贡献分收敛到 `packages/g64111/`，App/Server/MCP 通过同一本地包和各自独立的静态 fixture 精确对齐；四包、App/Server 全量测试、干净归档普通 `npm ci`、双生产镜像和镜像运行期包内容检查通过。真实 WorkBuddy `score.py` 兼容性仅 8/9，因此 M1 阶段门保持 PENDING，INT-201 未启动。 |
| 2026-07-12 | M1 阶段门通过：WorkBuddy 外部提交 `241e4a6` 修复小数 C1 兼容性，三份字节一致的 `score.py` 逐份通过共享 fixture 9/9；外部阻塞解除，INT-201 解锁为 READY，未在本更新中提前实施。 |
| 2026-07-14 | INT-201 完成：前端同实体写入严格串行、不同实体并行，文本草稿/拖拽合并、全局与实体保存状态、失败/冲突恢复、409 排队版本级联、统一认证清理和启动恢复入口落地；不具幂等保障的 ADD 网络未知结果禁止盲重试，通用 CommandRun/Idempotency-Key 按计划由 INT-202 接续。App 127 tests + build、G64111 27 tests、Server typecheck 全绿；INT-202 转 READY。 |
| 2026-07-14 | INT-202 完成：四类复合业务动作统一为带请求哈希的幂等事务命令；voice/recording 在外部模型调用前占用可恢复租约，正式落库、派生任务与快照保持原子；行动结果业务级一次性认领，收件箱五类批审一次提交，CommandRun 敏感摘要保持结构兼容脱敏。故障注入、并发重放、数据库竞争、跨租户/只读拒绝及全量 321 tests 和生产构建全绿；INT-203 转 READY。 |
| 2026-07-14 | INT-203 转为唯一 IN_PROGRESS；使用 `codex/int-203-workbuddy-sync` 与专用 worktree，按租户内业务锚唯一约束、原子 sync bundle、可重放 SyncReceipt 和旧工具一版兼容执行。 |
| 2026-07-14 | INT-203 完成：WorkBuddy 业务锚由数据库唯一约束与部署前冲突报告保护；客户、商机、拜访、人物/关系/Evidence 候选在一个可重放事务中同步，失败留隐私安全审计且可同 key 恢复；旧工具只读组装兼容响应，不再二次写库。多锚冲突、枚举、meta 溯源、候选容量、空库部署、并发与故障回滚回归均通过；Server 192 tests、App 128 tests + build、G64111 27 tests、PDE 25 tests 全绿，INT-204 转 READY。 |
| 2026-07-14 | INT-204 转为唯一 IN_PROGRESS；使用 `codex/int-204-job-recovery` 与专用 worktree，按 lease、条件 claim、指数退避、失败终态、崩溃恢复和入队去重执行。 |
| 2026-07-14 | INT-204 完成并通过 M2 阶段门：四类 Job 使用数据库去重与 Serializable 配额保护，多 worker 以 owner+token+lease 条件 claim；heartbeat、过期恢复、指数退避、三次失败终态与存量 processing 兼容落地。所有业务副作用在 fenced 事务内提交，人物候选与 Job done 原子完成；故障、晚到 worker、并发入队、配额、存量升级回归及全量 380 tests + production build 全绿，最终复审 CLEAN；INT-301 转 READY。 |
| 2026-07-14 | INT-301 转为唯一 IN_PROGRESS；使用 `codex/int-301-repair-tools` 与专用 worktree，按关键字段白名单、新旧父树失败关闭、冗余引用事务重绑、AuditEvent 闭环与复用归档/恢复语义执行。 |
| 2026-07-14 | INT-301 完成：客户/商机关键字段使用原值/版本 CAS 与真实 dirty-field 审计，legacy 负责人安全保留；VisitNote/Note 在新旧父树租户校验后事务重绑且重试无重复审计；归档/恢复复用既有语义，RepairPanel 提供来源、完整分页 SyncRun 与 AuditEvent 溯源并精确处理含冒号 sourceRef。Server 214 tests、App 137 tests + build、G64111 27 tests、PDE 25 tests 全绿，质量/安全复审 CLEAN；INT-302 转 READY。 |
| 2026-07-14 | INT-302 转为唯一 IN_PROGRESS；使用 `codex/int-302-person-merge` 与专用 worktree，按同租户同客户、全引用迁移、显式角色冲突决策、边去自环去重、真实人物归档、幂等事务和最小敏感审计执行。 |
| 2026-07-14 | INT-302 完成：同客户重复人物以 Serializable 幂等事务合并，双人物版本 CAS、全引用重定向、显式 OppRole 冲突决策、边自环/重复收敛、真实源人物归档、最小敏感审计与稳定 UI 重试键落地；Edge/RelSuggestion 所有 formal/candidate endpoint 在首写前按租户/客户/active 父树失败关闭。Server 227 tests、App 140 tests + build、G64111 27 tests、PDE 25 tests、PostgreSQL 生产镜像全绿，最终独立复审 CLEAN / READY；INT-303 转 READY。 |
| 2026-07-14 | INT-303 转为唯一 IN_PROGRESS；使用 `codex/int-303-token-scopes` 与专用 worktree，按服务端预设、实时 token/user/role 复核、集中 tool scope 矩阵和无副作用失败关闭执行。 |
| 2026-07-14 | INT-303 完成：三套服务端 Token 预设、legacy v0 安全只读迁移、每请求 token/user/role 复核、集中 MCP tool scope 与动态 bundle 门禁落地；viewer 读取复用稳定负责人投影并过滤 private BI/UCV 评分侧信道。Server 239 tests、App 141 tests + build、G64111 27 tests、PDE 25 tests、双 provider Prisma 生成/校验全绿，最终独立规格/质量/安全复审 CLEAN；INT-304 转 READY。 |
| 2026-07-14 | INT-304 转为唯一 IN_PROGRESS；使用 `codex/int-304-workbuddy-e2e` 与专用 worktree，按真实 HTTP 两阶段同步、候选人审、Evidence→PDE 快照、行动回填、重放与租户隔离执行，只补契约回归和文档，不改生产业务代码。 |
| 2026-07-14 | INT-304 完整 HTTP 旅程除行动回填 `AuditEvent` 外全部通过；精确 RED 为期望 1 条事务内审计、实际 0。因最小审计补丁需修改生产事务代码，任务转 BLOCKED 并记录 ADR，M3 阶段门与 INT-401 保持锁定，等待项目所有者决定补审计或调整验收契约。 |
| 2026-07-14 | 项目所有者批准 ADR-INT-304 选项 A；行动回填在 PlanAction/Evidence/completed CommandRun 同一事务写一条脱敏 `AuditEvent`，并补 `flat`、无人物、不同 key、跨租户和审计后故障回滚回归。focused 19 tests 已转绿，INT-304 恢复 IN_PROGRESS 等待全量门禁。 |
| 2026-07-14 | INT-304 完成并通过 M3 阶段门：虚构 fixture 的 WorkBuddy 两阶段同步/重放、候选人审、Evidence→PDE 快照、行动回填及最小审计全链通过；同/不同 key 并发、故障回滚、Token/viewer 拒绝和跨租户无副作用均有回归。Server 244 tests、App 141 tests + build、G64111 27 tests、PDE 25 tests、SQLite Prisma 与 PostgreSQL 镜像全绿，最终红队 `NO_BLOCK`；中央幂等 key 哈希兼容迁移与 opaque ID 契约登记至 INT-502 发布前加固，INT-401 转 READY。 |
| 2026-07-14 | INT-401 完成：ADURC 统一为 A批准人/D拍板人/U使用者/R影响者·技术把关/C教练，C5 可写契约仅接受五个权威键且存量别名只读兼容；主 D 仅能选择同租户同客户同商机的当前 D，人物改角/删除/合并同步维护引用；P4 仅限非 A/D，提案采纳复用 Serializable + P2034 重试，历史非法或重复选择在下一次角色写入时收敛；Web 乐观态与 REMOVE_ROLE 撤销重做复用同一约束，DELETE_PERSON 因客户端快照受 ACL/字段裁剪而明确不进入撤销历史，存量 TB/root 建议角色在人审前失败关闭。G64111 32 tests、domain-contracts 13 tests、Server 263 tests、App 153 tests + build、SQLite Prisma 与 PostgreSQL 生产镜像全绿；M4 阶段门保持未通过，INT-402 转 READY。 |
| 2026-07-15 | INT-401 二次复审 4 个 Important 关闭：共享稳定合法 P4 keeper（按 personId，仅 U/R/C）统一 scoring、角色收敛、商机 clone、PDE 与 App gaps；clone 严格限定 tenant/account/opportunity 父树并排除越界人物；C5 共享 canonical 读取/归一化覆盖旧三别名的渲染、缺口、MD 往返与回写；DELETE_PERSON 继续清主 D，但不再从裁剪快照伪造 inverse。focused RED 均精确复现并转绿，全量与双 provider 证据同上。 |
| 2026-07-15 | INT-401 最终整分支复审 4 个 Important 关闭：FORM 缺口与评分共用有效显式主 D/首 D fallback；P4 提案预览模拟真实单选写入并对 A/D 失败关闭；AI 上下文只标记一个合法 P4 keeper 与一个主 D，内置分析优先这些规范化标记；两份 WorkBuddy 写入文档统一 ADURC、P4 与 C5 权威口径并由扫描回归禁止旧角色串。最终 G64111 32、domain-contracts 13、App 159 + build、Server 265、SQLite Prisma 与 PostgreSQL 生产镜像全绿；INT-403 可见范围最小化未提前实施。 |
| 2026-07-15 | INT-401 最终复审补充关闭可选 selection 误标：AI context 在主 D/P4 keeper 均不存在时不再因 `undefined === undefined` 把无角色人物标为 true；空 roles 与普通 U/R/C 无 P4 两组 RED 精确复现，显式存在性保护后 focused 4/4 与 App typecheck 全绿。App 总数更新为 161，INT-403 范围仍未提前实施。 |
| 2026-07-15 | INT-402 完成：业务日期统一由 `localYmd` 按 Asia/Shanghai 生成，MomentFlow 保留全部未完成逾期行动并纳入 Evidence-only 人审；移动审核等待服务端成功、失败留卡可重试，拜访录入携带客户/商机/人物明确上下文；批审逐项显示进度、成功/失败和失败项，成功项从重试选择中移除。focused 11、App 173 tests、typecheck 与 production build 全绿；UTC 截断扫描、敏感文件/PDE 范围扫描和提交前审查通过。M4 阶段门保持未通过，INT-403 转 READY。 |
| 2026-07-15 | INT-402 最终复审 5 个 Important 关闭：voice `personId` 按 tenant+account+opportunity 父树在命令预留前与事务内双重失败关闭，不允许模型绕过人审；批审单项稳定 key 在人工/网络重试中复用，成功清理而失败保留；DeliberationDock 按 Asia/Shanghai 统一 action/doneAt/evidence/逾期业务日与无设备时区漂移的日期加减；无效拜访上下文禁用并提示重选；`localYmd` 无效 Date/时区 `RangeError` 契约明确。最终 App focused 31 / 全量 186 + typecheck/build、Server focused 4 / 全量 269 + typecheck 全绿；INT-403 恢复 READY，M4 阶段门仍未通过。 |
| 2026-07-15 | INT-402 整分支复审 3 个 Important + 1 个 Minor 关闭：Server 业务日集中为 Asia/Shanghai，覆盖 voice/单审/批审/巡检/doneAt 并清零 App+Server UTC 截断；MomentFlow 跨客户确定性排序；voice TOCTOU scoped 404 精确撤销本请求仍 running 的 reservation，完整保留普通业务失败记录；logout 清理拜访/批审临时态。最终 App 25 files / 188 tests + typecheck/build、Server focused 4 files / 34 tests + 全量 33 files / 280 tests + typecheck 全绿；INT-403 恢复 READY，M4 阶段门仍未通过。 |
| 2026-07-15 | INT-402 最终复审补充 1 个 Important + 1 个 Minor 关闭；logout 同步清空 tenant-scoped inbox，restore/auth switch/logout 每次递增 session generation，所有 inboxList 返回均双重复核 generation+token，A 用户延迟响应不能覆盖 B；patrol 对非法 start/end date 产生的非有限 day distance 失败关闭，不生成“逾期 NaN 天”提醒。最终 App 25 files / 190 tests + typecheck/build、Server focused 4 files / 35 tests + 全量 33 files / 281 tests + typecheck 全绿；INT-403 恢复 READY，M4 阶段门仍未通过。 |
| 2026-07-15 | INT-402 最终复审请求归属 Important 关闭：API request 发起时捕获最终 Authorization 的实际 Bearer token，401 仅在该 token 仍等于当前 session token 时广播 unauthorized；A 延迟 401 不登出 B，旧匿名 401 不登出后续登录，当前 B 401 仍精确广播一次并执行清理。最终 App focused 2 files / 15 tests、全量 25 files / 193 tests + typecheck/build 全绿；Server 未改动不重跑；INT-403 恢复 READY，M4 阶段门仍未通过。 |
| 2026-07-15 | INT-402 最终 Headers 归属 Important 关闭：request 先以 `new Headers(opts.headers)` 规范化合法 HeadersInit，只有最终 Headers 缺 Authorization 时才补全局 bearer；实际 bearer 快照与 fetch 共用同一 Headers 实例，避免 Headers 对象字段丢失及大小写重复合并。Headers 自定义 bearer、大小写覆盖、Basic/空 Authorization、A/B/匿名/current B 401 均有回归。最终 App focused 2 files / 19 tests、全量 25 files / 197 tests + typecheck/build 全绿；Server 未改动不重跑；INT-403 恢复 READY，M4 阶段门仍未通过。 |
| 2026-07-15 | INT-403 完成并关闭最终隐私复审：AI simulate 与四个 strategy 模型入口收口到服务端 tenant+actor+opportunity 可见范围；权威 manifest token 绑定 tenant/actor/opportunity/标准化 options，同形范围不可跨主体重放，foreign tenant 404 不泄密、viewer 403；preflight 是 UI manifest 唯一写入源，Advisor/Dock 的模型结果、错误、候选采纳与 draft dispatch 均按 scope generation 失败关闭，切客户/商机/焦点人/选项不会把旧结果落入新页面。Dock 进一步用 target/person/输入指纹/nonce 守卫抽屉预填、策略卡派发和里程碑排行动，切换、编辑或删除均丢弃旧响应且不清除新 busy。Disclosure 显示实际日志数，private BI/self 日志/非当前商机始终排除，raw logs/FORM 默认关闭；内嵌与完整手册改为真实内部流程。App 209 tests + typecheck/build、Server 286 tests + typecheck、G64111 32 tests 全绿；INT-404 转 READY，M4 阶段门保持未通过。 |
| 2026-07-15 | INT-404 完成：认证提交前中文校验、语义化自动填充与错误 live region 已落地；Modal 具备标准 dialog 语义、焦点环、Escape 和稳定焦点归还，竖屏引导对工作台执行 inert+aria-hidden 隔离；核心客户/评分/文档/沙盘卡片支持键盘。真实浏览器覆盖 auth/workbench 320/375/768/1440、竖屏引导和弹窗直接/下拉入口，均无横向文档溢出且焦点行为通过。App 216 tests + typecheck/build、Server typecheck、G64111 32 tests 全绿；M4 四项阶段门通过，INT-501 转 READY。 |
| 2026-07-15 | INT-501 完成并关闭复审的 7 Important + 2 Minor：备份改为独立强 master secret、域分离 encrypt-then-MAC 与认证目录原子发布；并发锁/随机名、生产库运行态识别、严格隔离库前缀、system DB 拒绝、分连接 terminate/drop 与删除断言均落地。公司 pre-INT501 bridge 在 pull/build 前执行真实加密备份和隔离恢复，部署 readiness/卷识别失败关闭，备份制品移出源码及 Docker context，生产旧 `db push`/删卷指引全仓去除。隔离 PostgreSQL bootstrap、双并发备份、恢复/replace、WorkBuddy 数据、错 key/篡改/坏归档清理全绿；Server 304、App 216 + build、G64111 32、PDE 25、双 Prisma validate 与生产镜像全绿。INT-502 转 READY，M5 发布门保持未签署。 |
| 2026-07-15 | INT-501 二次复审 6 Important + 1 Minor 关闭：Compose 资源探测与 PostgreSQL 存在查询改为错误不可冒充 absent 的三态；restore/bootstrap 的查询、smoke、terminate/drop 和 absent 断言统一 critical cleanup。bootstrap marker 原子发布并严格绑定部署身份和认证备份，update 在 pull/build 前重验。备份锁使用 nonce+PID start owner、串行 stale quarantine 与 owner compare 防 ABA；周期/常见模式 master secret 拒绝。behavioral focused 13/13、真实 PostgreSQL smoke 故障清理/query error 70/marker/双并发/恢复失败全绿；Server 308、App 216 + build、G64111 32、PDE 25、双 Prisma 与生产镜像全绿。INT-502 恢复 READY，M5 发布门保持未签署。 |
| 2026-07-15 | INT-501 最终锁/密钥复审关闭：operation guard 不再自动回收，现有 guard 只 bounded wait 后失败关闭，避免双 reaper 搬走 successor；主锁 stale quarantine 仍严格在持 guard 并重读 owner 后执行，延迟 owner cleanup 不删除 successor。master secret 周期检测补齐 period 32。focused RED 2 项精确复现后 14/14 转绿，真实 PostgreSQL 全链与 Server 35 files / 309 tests + typecheck 再次通过。INT-502 恢复 READY。 |
| 2026-07-15 | INT-502 启动 RC 准备：中央 CommandRun key SHA-256 兼容迁移、128-bit opaque ID 生成、Person 创建时间指标基数、三套脱敏旅程、全包依赖清零、CI 扩门、受控运行报表及显式生产回滚路径已实现。预冻结门禁通过 Server 37 files / 315 tests、App 27 / 217 + build、Domain 14、G64111 32、PDE 25、全依赖/生产依赖 0 漏洞、真实 PostgreSQL 混合存量迁移与备份恢复。状态仍为 IN_PROGRESS/NO-GO；须推送并部署最终 RC、完成真实回滚演练、三商机人工签名、连续 336 小时指标与 Owner 签署。 |
| 2026-07-16 | INT-502 RC 预推送隔离回滚演练通过：固定运行中数据库/旧镜像回滚点后写入快照外记录，显式回滚恢复到隔离库并重启旧镜像；readiness 通过，恢复后快照前记录 1、快照后记录 0，临时 Compose 容器、卷和备份目录清理完成。该证据只验证回滚实现，不替代公司服务器正式演练；INT-502 保持 IN_PROGRESS/NO-GO。 |
| 2026-07-16 | INT-502 RC 远端门禁首轮发现并关闭 Server CI-only 测试污染：跨类型去重键用例错误地用并发 SQLite 写事务验证非并发目标，在 GitHub runner 上先触发 5 秒超时，再因 cleanup 超时污染后续 Job claim。受限单核 Linux 容器复现修复前约 15 秒超时；改为顺序验证四类 key 后同环境 186ms 通过，并发同 key 与 49→50 配额竞争仍由相邻专用用例覆盖。Server 37 files / 315 tests + typecheck 全绿，等待新 SHA 远端 CI 复核；INT-502 保持 IN_PROGRESS/NO-GO。 |
| 2026-07-16 | INT-502 候选代码 `d627d31e27a16355ebab3ad13d57ccb607499587` 的远端自动发布门全绿：Server 315 tests、App/Domain/G64111/PDE 类型与测试、App/生产镜像构建、真实 PostgreSQL 运维集成以及五包完整/生产依赖审计全部通过。自动门五项勾选；RC 冻结后等待公司服务器正式回滚、三商机人工签名、连续 336 小时指标与 Owner 签署，状态保持 IN_PROGRESS/NO-GO。 |
| 2026-07-20 | INT-502 公司旧库 bootstrap RC5：只读盘点确认生产库 41 张业务表有 `Tenant`/`User`/`Account`、无后期 `SyncRun`，证实 RC4 旧库 readiness 仍绑定了错误版本。RC5 改用初始稳定核心表，并比较源库与隔离恢复库的有序 `public` 表清单签名；普通恢复严格档位、隔离目标限制、失败清理和 marker 门禁不放宽。TDD RED 后 focused 19/19、Server 全量与 typecheck 通过，代码提交 `0cacf8e`；等待公司服务器真实重跑，状态保持 IN_PROGRESS/NO-GO。 |
| 2026-07-20 | INT-502 公司旧库 migration RC6：三轮推送前审查确认 RC5 虽能恢复 41 表旧库，但不能安全接管当前 45 模型，且部分/失败 migration 历史、唯一索引冲突、负责人稳定 ID、停服信号、脏 bootstrap、回滚后运行 SHA 均有发布风险。RC6 固化 2026-07-12 旧模型并新增幂等 bridge；bootstrap 在隔离库完成 migration、零漂移、readiness 且构建输入干净后才写 marker。bridge 完成前绑定候选 commit，事务中断可 rolled-back 后安全重放；唯一索引前扫描同步锚/企微冲突；迁移后唯一同名负责人自动回填、歧义失败关闭；停服失败/信号恢复服务；部署及回滚均原子更新运行 SHA。64-hex raw/digest 歧义失败关闭。TDD RED 后 focused 54/54、Server 319 + typecheck、App 217 + build、G64111 32、PDE 25、双 PostgreSQL schema validate、脚本语法与 diff check 通过，主体 `6776e93`、重入 `577e7d7`、revision gate `3219a19`、中断安全 `2800079`、bridge 恢复 `cbf1fbc`；等待真实 Docker CI 和公司服务器 E2E，状态保持 IN_PROGRESS/NO-GO。 |
| 2026-07-20 | INT-502 RC6 最终生命周期复审关闭 2 个 P1：一次性负责人回填只在未纳管/中断的 schema 接管中执行，普通 entrypoint 重启不再因后续重名用户失败；update 在停写前记录 migration history，失败或信号后 history 未改变才恢复旧容器，已改变或无法确认则自动执行认证回滚，禁止旧 `db push` 容器连接新 schema。TDD RED 后 focused 34/34、脚本语法与 diff check 通过，代码提交 `fb2cce6`；等待远端 CI 和公司服务器 E2E，状态保持 IN_PROGRESS/NO-GO。 |
| 2026-07-20 | INT-502 RC6 重启绕过复审关闭 1 个 P1：负责人歧义/越租户稳定 ID 检查与唯一姓名回填从 entrypoint 后置步骤移入 bridge migration 同一事务；失败时新增列、表和负责人写入整体回滚，`restart: unless-stopped` 无法在下一次启动跳过。真实 PostgreSQL 集成脚本新增唯一姓名回填及“重名失败回滚→修复→安全重放”门禁；TDD RED 后 focused 34/34 与脚本语法通过，代码提交 `f7adc58`；等待远端 Docker CI 和公司服务器 E2E，状态保持 IN_PROGRESS/NO-GO。 |
| 2026-07-20 | INT-502 RC7 冻结：RC6 首轮远端门仅因冷启动数据库尚未接受连接而在 Prisma 前触发 `P1001`，其余 job 全绿；`ccd7ee8` 在真实 PostgreSQL 集成脚本中加入有界 `pg_isready` 就绪等待并以静态回归锁定调用顺序。GitHub Actions run `29718040962` 的 Server 319、App 217 + build、Domain/G64111/PDE、五包双依赖审计、生产镜像和真实 PostgreSQL 运维全链均通过。自动门已关闭；等待公司服务器 bootstrap/正式回滚演练、三商机人工签名、连续 336 小时指标与 Owner 签署，状态保持 IN_PROGRESS/NO-GO。 |
| 2026-07-20 | INT-502 RC8 冻结：公司 CentOS 7 旧 Git 在 bootstrap 第一处 `git -C` 返回 `Unknown option: -C`，工作树安全门因此拒绝继续；数据库、备份和 migration 尚未变更。`765dc80` 用子 shell 切目录兼容旧 Git，并让真实运维演练复用同一辅助函数；首轮 CI 暴露演练脚本漏 source 后以 `bd37656` 补齐并锁定 source 顺序。RED→GREEN focused 20/20、本地 Server 320 + schema/typecheck、全部 shell 语法通过；GitHub Actions run `29725028925` 的 Server/App/Domain/G64111/PDE、五包双依赖审计、生产镜像和真实 PostgreSQL 运维全链均通过。等待公司服务器 bootstrap/正式回滚演练、三商机人工签名、连续 336 小时指标与 Owner 签署，状态保持 IN_PROGRESS/NO-GO。 |
| 2026-07-21 | **重大偏差 ADR-INT-502 批准并执行**：项目所有者确认公司旧库无需保留数据，投产路径改「备份双份留档→清库→fresh install」，验收简化（三旅程→一套冒烟清单、336h→48–72h、样本 100→20 条）。配套完成 INT-503（工作树修复 npm ci --install-links 防复发条款、MomentFlow 退役、EngineBar/WinTendencyPanel/PersonForm 僵尸组件删除、孤儿 CSS ~145 行清理、CLAUDE.md 对齐 AGENTS.md、过时文档归档/状态头、57→3 分支与 33→1 worktree 清理均含 archive/ 标签兜底）与 INT-504（update.sh 路径参数化+空库首装自动写认证备份与 bootstrap marker；集成测试新增 fresh-install 双遍场景并在本机真实 Docker 全链通过 FRESH_INSTALL_FIRST_RUN_OK/SECOND_UPDATE_OK）。releaseMetrics 常量 336→48/100→20、阈值键 observed336Hours→observedRequiredHours。App 205 tests（MomentFlow 自带 12 项随退役移除）+ Server 321 + G64111 32 全绿。INT-502 保持 IN_PROGRESS/NO-GO，等待 rc10 冻结与公司服务器清库重装执行。 |
