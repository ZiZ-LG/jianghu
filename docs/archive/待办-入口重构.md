# 待办 · 入口重构（✅ 全部落地）

> 本文件由对话生成，记录「这次对话之后应该做的事」。动手前务必先读「环境教训」。
>
> **✅ 状态更新（2026-07-01）**：B1（删心跳+报表 `8e9aeef`）+ B2（Hub 顶栏 12→5 · ⚙️设置折叠 `75faf14`）**均已落地**；「后续」的 喂料口三合一 → 已由 `AddIntel.tsx` 实现（地图页「＋添加情报」单入口 `8823b4f`）、地图顶栏 ❓帮助/🌙主题 折进 ⚙️（`75faf14`）也做了。**本文所列全部完成，保留作历史。**

## 现状（截至本次对话）

- ✅ **B1 第一刀已落地且 `tsc` 绿**：
  - `app/src/App.tsx`：删心跳 `● 监测中` + 删报表（import / state / 桌面按钮 / 移动 OverflowMenu item / 渲染块）；主题按钮补 `marginLeft:auto`。
  - 删组件文件 `app/src/components/ReportPanel.tsx`。
  - git 现状：`M app/src/App.tsx`、`D app/src/components/ReportPanel.tsx`。
- `app/src/components/CustomerHub.tsx` 已 `git checkout` 回退为**原始**（第二刀未做）。

## ⚠️ 环境教训（务必先读）

- 本次会话**上下文溢出**，导致大文件 `Edit` 出现「假 success」（回执报成功、实际没落地），后期连 `ls`/`grep` 只读复核都返回**自相矛盾**的结果。
- 已排除 git 锁 / 外接卷 `/Volumes` / 磁盘空间（`git status`、`df` 均正常）。根因 = 会话过长。
- **纪律**：① 动手 B2 前先 `/compact` 或开新会话恢复工具可靠性；② 每次编辑后用 `git diff <file>` 复核**真实落地**，绝不信回执；③ 一旦发现「假 success」，立即停手 `/clear`。

## 待做 · B2 第二刀（Hub 集约，全在 `app/src/components/CustomerHub.tsx`）

### ① `submit` 函数后、`return` 前，插入
```tsx
  // 配置类聚合到「⚙️ 设置」——低频，不与日常动作抢位（桌面/移动共用）
  const settingsItems = [
    { label: '🔌 接入 AI', onClick: onOpenMcpAccess },
    { label: '🧠 AI 模型', onClick: onOpenAiSettings },
    { label: '📆 企微日历', onClick: onOpenWecom },
    { label: '👥 团队 · ❤️ 支持', onClick: onOpenTeam },
    { label: theme === 'dark' ? '☀️ 白天模式' : '🌙 黑夜模式', onClick: onToggleTheme },
    { label: '📋 载入示例', onClick: onLoadDemo },
    { label: '❓ 帮助', onClick: onOpenHelp },
    { label: '🚪 退出登录', onClick: onLogout },
  ];
```

### ② 桌面 `hub-actions-desktop` 整块替换为（12 → 5）
```tsx
        <div className="hub-actions-desktop">
          <span className="who">{userName}</span>
          <button className="team-chip inbox-chip" onClick={onOpenInbox} title="审核机器写入的候选（关系 / 人物），采纳后才落库">📥 收件箱{inboxCount > 0 ? ` · ${inboxCount}` : ''}</button>
          <button className="btn cta" onClick={onOpenIntel}>🎙️ 录入情报</button>
          <button className="btn primary" onClick={() => setCreating(true)}>＋ 新建客户</button>
          <OverflowMenu align="right" label="⚙️ 设置" items={settingsItems} />
        </div>
```

### ③ 移动 `hub-actions-mobile` 整块替换为（删独立主题按钮）
```tsx
        <div className="hub-actions-mobile">
          <button className="btn primary xs" onClick={() => setCreating(true)}>＋ 新建</button>
          <OverflowMenu align="right" label="⚙️" items={[
            { label: '📥 收件箱', badge: inboxCount > 0 ? String(inboxCount) : undefined, onClick: onOpenInbox },
            { label: '🎙️ 录入情报', primary: true, onClick: onOpenIntel },
            ...settingsItems,
          ]} />
        </div>
```

### 验收
```bash
cd app && npx tsc --noEmit   # 应绿
```

## 可选 / 后续

- 先 commit 锁定第一刀：`git add -A && git commit -m "refactor(入口): B1 删心跳+报表"`
- B2 之后的批次：侧栏「喂料口」三合一（录入情报 + 录音 + 自算 + 和地图对话 → 一个入口）；地图顶栏 `❓帮助`/`🌙主题` 也折进一个 ⚙️。
- 参考记忆：`jianghu-entry-refactor`（如已写成）、`jianghu-interaction-upgrade`。
