# SAAS-210 可选 G64111 模板安装与无方法论销售闭环执行计划

> **执行方法：** 使用 `superpowers:executing-plans` 按检查点逐项实施；生产代码遵循 `superpowers:test-driven-development`，完成声明前使用 `superpowers:verification-before-completion`。

**任务：** SAAS-210

**目标：** 让有 `methodology.g64111` entitlement 的商业租户显式安装租户本地 G64111 快照，并按 Matter 显式绑定或解绑；未安装、未绑定或绑定其他方法论时，方法论中立的复杂销售闭环继续工作，G64111 评分、缺口、角色视图和冻结的重工作台均不运行。

**基线：** `origin/main@4f7573c72618b2043d84696dd559f3f552f46dbf`；PR #44 合并后的精确 SHA CI [33742990587](https://github.com/ZiZ-LG/jianghu/actions/runs/33742990587) 12/12 jobs 成功。

**工作区：** `/Volumes/PowerData/江湖APP/.worktrees/saas-210-methodology-template`

**分支：** `codex/saas-210-methodology-template`

**架构边界：** 复用 CORE-112 已存在的 `MethodologyPack`、不可变 `MethodologyPackVersion`、追加式 `MethodologyBinding`、Matter 单一 active pointer 和四个受审计命令，不新增 schema 或 migration。新增一个方法论安装/绑定只读投影作为 UI 唯一状态源。商业版冻结重工作台只允许打开“当前 active binding 精确属于 G64111 内置快照”的 Matter；内部版 legacy shell 行为保持不变。

**明确批准的共享文件：** `app/src/App.tsx`。除此之外不修改 `app/package.json`、lockfile、Vite、`app/dist/**`、`docker-compose.yml`、根级 scripts、公共 CI/Nginx 或主站导航；若实现需要其他共享文件，立即暂停并请示。

**永久约束：** 所有读取 tenant-scoped 并使用当前数据库角色的 `EffectiveResourceScope`；viewer 继续只见归属 Customer 且不能执行写命令；AI 不参与安装/绑定和正式写入；不触碰自我修养、生产、阿里云或 Mac mini。

---

## 1. 权威契约与只读投影

### Files

- Modify: `packages/domain-contracts/src/methodology.ts`
- Test: `packages/domain-contracts/tests/methodology.test.ts`

### Steps

- [x] 先增加失败测试，固定 G64111 内置身份常量和严格只读 DTO：安装状态、当前操作者是否可管理、命令开关、可见 Customer/Matter、Matter version 与 active binding 元数据。
- [x] DTO 禁止出现 `primaryDPersonId`、`roles`、`c3Items`、`c5Items`、`pipelineStage`、`engageStage`、评分或预测字段。
- [x] active binding 必须携带可验证的 `packKey`、`sourceTemplateRef`、`versionKey` 与 `engineRef`；客户端只能以精确内置身份判断 G64111 active，不能把 entitlement 或任意方法论绑定当作 G64111。
- [x] 对重复 Matter、父级不一致、安装 pack/version 不一致和 malformed 数据做 schema 失败测试。
- [x] 实现最小 Zod schema/type/常量并转绿。

预期核心判断：

```ts
export function isG64111Active(binding: MethodologyActiveBindingSummary | null): boolean {
  return binding?.packKey === G64111_BUILTIN_PACK_KEY
    && binding.sourceTemplateRef === G64111_BUILTIN_SOURCE_TEMPLATE_REF;
}
```

### Verify

```bash
cd packages/domain-contracts
npm test -- methodology.test.ts
npx tsc --noEmit
```

---

## 2. Tenant-scoped G64111 安装/绑定读取入口

### Files

- Create: `server/src/methodology/readModel.ts`
- Create: `server/src/methodology/routes.ts`
- Modify: `server/src/app.ts`
- Create: `server/tests/methodology-read-model.test.ts`

### Steps

- [x] 先写失败集成测试：无安装时只返回方法论中立 Matter；已安装/绑定时只标记精确 G64111 active Matter；绑定其他 pack 不得被误判。
- [x] 加入跨租户、scoped member、viewer Customer 归属、当前数据库角色变化、归档父级、伪造/悬空 active pointer 的失败关闭测试。
- [x] 测试查询只 select 中性字段，带“投毒”的 legacy G64111 字段不会进入返回值或影响结果。
- [x] 实现 `GET /api/methodology/g64111`：认证、`private, no-store`、`methodology.g64111` capability 门；用当前数据库角色解析 `EffectiveResourceScope`，所有查询显式带 `tenantId`。
- [x] 安装只接受本租户 `platform.g64111` + `builtin:g64111:1` + current published immutable version 的一致快照；active pointer 与 binding/opportunity/pack/version 任一不一致均返回稳定 409，绝不猜测或 fallback。
- [x] `canManage` 只由当前数据库角色 owner/admin 决定；`commandsEnabled` 仅暴露布尔配置，不泄露环境内容。
- [x] 在 `server/src/app.ts` 将 GET 与现有 methodology command 都明确归类到 `methodology.g64111`，避免依赖默认 sales adapter 分类。

### Verify

```bash
cd server
npm test -- methodology-read-model.test.ts methodology-command.test.ts
npx tsc --noEmit
```

---

## 3. 前端解析、命令与无方法论 fail-closed helper

### Files

- Create: `app/src/lib/g64111Setup.ts`
- Create: `app/src/lib/g64111Setup.test.ts`
- Modify: `app/src/api.ts`
- Modify: `app/src/api.test.ts`

### Steps

- [x] 先写失败测试，证明：entitlement 本身不等于安装或绑定；无 pack fixture 的 G64111-bound ID 集合为空；绑定其他方法论仍为空；只有精确 G64111 binding 被纳入。
- [x] 固定商业 legacy workroom 和 G64111 Today/score/gaps 共用同一个 exact-bound predicate，避免不同消费者各自猜测。
- [x] 为 materialize/bind/unbind 生成 opaque ID 和命令 payload；bind/unbind 必须携带 Matter version 与 expected active binding CAS。
- [x] 在 `app/src/api.ts` 增加严格解析的 GET 和 methodology command client；验证 `replayed`、receipt action、Matter/pack/version/binding ID 与请求精确一致，畸形/错配响应一律 `invalid_response`。
- [x] 命令继续复用现有固定 token、固定 `Idempotency-Key` 的安全重试路径。

### Verify

```bash
cd app
npm test -- src/lib/g64111Setup.test.ts src/api.test.ts
npx tsc --noEmit
```

---

## 4. G64111 可选安装与 Matter 绑定 UI

### Files

- Create: `app/src/components/G64111SetupPanel.tsx`
- Create: `app/src/components/G64111SetupPanel.test.ts`
- Modify: `app/src/components/CommercialShell.tsx`
- Modify: `app/src/components/CommercialShell.test.ts`

### Steps

- [x] 先写渲染失败测试覆盖 loading/error、未安装、已安装未绑定、G64111 active、其他方法论 active、命令关闭、viewer/read-only。
- [x] `/g64111` 不再因 entitlement 显示“已就绪”；owner/admin 可显式安装、绑定、切换或解绑，viewer/member 只读查看状态。
- [x] 任何按钮 busy 时防重复；失败保留稳定状态并提示刷新，不做乐观伪绑定。
- [x] 成功后用服务端 receipt 通过 API 精确校验，再刷新读取投影；不直接改本地正式业务状态。
- [x] 复杂销售页面的冻结 legacy 入口只列出 exact G64111-bound Matter，并把 `{customerId, matterId}` 传给 App；无绑定时明确说明通用工作流仍可使用。
- [x] 加入“无方法论复杂销售 fixture”：拜访前简报、会后速审、关系工作台和 Matter 组合入口仍渲染；页面不出现 G64111 专有评分/角色/缺口或“已就绪”文案。

### Verify

```bash
cd app
npm test -- src/components/G64111SetupPanel.test.ts src/components/CommercialShell.test.ts
npx tsc --noEmit
```

---

## 5. App 单点绑定门与会话生命周期

### Files

- Modify (owner-approved shared file): `app/src/App.tsx`
- Create: `app/src/lib/g64111AppBoundary.test.ts`

### Steps

- [x] 先用纯 helper/边界测试固定：商业 shell 的未绑定 Matter 不得调用 G64111 Today、`scoreFromDomain` 或 `computeGaps`；内部 legacy shell 保持原行为。
- [x] App 为商业会话加载 G64111 setup read model；使用现有 session guard/latest-request 模式，登出、换租户、旧请求晚到时清空/拒绝旧状态。
- [x] 方法论读取失败时 fail closed：通用商业 shell 继续可用，但 G64111 专有计算和冻结 workroom 不可进入；错误可重试。
- [x] `computeG64111Today` 只接收 exact-bound Matters 的投影；`scoreFromDomain` 和 `computeGaps` 只在当前 Matter exact-bound 时运行。
- [x] 商业版深链、legacy 按钮和选中状态只能打开 exact-bound `{customerId, matterId}`；未绑定/错误 parent/读取未就绪时返回 commercial shell。内部版深链不受影响。
- [x] setup 安装/绑定/解绑后同时刷新 setup read model；必要时刷新 legacy state 取得最新 Matter version，保持 CAS 正确。
- [x] 不修改主站导航、自我修养路由或任何部署逻辑。

### Verify

```bash
cd app
npm test -- src/lib/g64111AppBoundary.test.ts src/components/CommercialShell.test.ts src/components/G64111SetupPanel.test.ts src/api.test.ts
npx tsc --noEmit
npm run build -- --outDir "$(mktemp -d)/saas-210-app-dist"
```

---

## 6. 两套 fixture、回归与安全门

### Files

- Test: `server/tests/methodology-read-model.test.ts`
- Test: `app/src/lib/g64111Setup.test.ts`
- Test: `app/src/components/CommercialShell.test.ts`

### Steps

- [x] Fixture A：无安装/无绑定且 legacy 专有字段带非默认“投毒”值；通用 Customer/Matter、拜访前/会后、关系/假设、雷达/组合与行动草稿可达；G64111 专有消费者为 0。
- [x] Fixture B：显式物化内置模板 → 对单个 Matter 激活 binding → 该 Matter 可进入冻结 workroom并运行 G64111 adapter；同客户未绑定 sibling Matter 仍不可进入。
- [x] Fixture B 再执行解绑：Matter/Customer/Person/Relation/Commitment 不删除，版本按 CAS 增加，历史 binding 与审计保留，G64111 专有入口立即消失。
- [x] 重放同一 idempotency key 返回同一 receipt 且不重复创建 pack/binding/audit。
- [x] 检查变更不含自我修养路径、不含未批准共享文件、不含 secret/DB/build artifact。

### Full local verification

```bash
cd packages/domain-contracts && npm run typecheck && npm test
cd packages/g64111 && npm run typecheck && npm test
cd packages/pde-kernel && npx tsc --noEmit && npm test
cd app && npm ci --install-links && npx tsc --noEmit && npm test
cd server && npm ci --install-links && npm run generate && npx tsc --noEmit && npm test
cd server && npm run schema:postgres:check
git diff --check
```

`packages/*` 变更后必须先刷新 App/Server 的 `file:` 安装副本再执行最终验证。App build 只输出到 `mktemp`，禁止写 `app/dist/**`。

---

## 7. 原子提交、远端门与状态收口

### Commits

- [x] 治理启动提交：本计划 + 清单 `SAAS-210 READY→IN_PROGRESS`。
- [x] 业务提交：契约、只读路由、前端 API/helper/UI、已批准的 `app/src/App.tsx` 和测试；不混入治理收口。
- [x] Push feature branch，确认 GitHub Actions 对应业务提交精确 SHA 12/12 jobs 全绿。
- [x] 业务 SHA 通过后，独立治理提交把 SAAS-210 标为 `DONE`、SAAS-211 标为 `READY`，记录测试数、CI URL、回滚边界和零生产触碰；push 并确认精确治理 SHA CI 全绿。
- [x] 检查远端 PR 状态；若需创建新 PR，按项目所有者既有门禁单独报告并等待批准，不合并、不部署。

### Completion evidence

- Business commit: `3c4fe4361087f657d7391a1a2801f6e8722842cc`; [GitHub Actions run 33764644921](https://github.com/ZiZ-LG/jianghu/actions/runs/33764644921) completed with 12/12 jobs successful for that exact SHA.
- Local gates: Domain Contracts 17 files / 160 tests; Server 122 files / 964 tests plus Prisma generate, typecheck and PostgreSQL schema render check; App 60 files / 408 tests plus typecheck and production build outside `app/dist`; G64111 2 files / 32 tests; PDE 3 files / 25 tests.
- Scope: 18 business files in App, Server and Domain Contracts; no schema, migration, package/lock/Vite, Docker Compose, root script, workflow, deployment, homepage/navigation or self-cultivation change. The only shared/high-conflict file was the owner-approved `app/src/App.tsx`. Production, Aliyun, Mac mini and `main` were not touched.
- Rollback: hide the `/g64111` setup and commercial frozen-workroom entry, then revert the business commit. Preserve every tenant pack/version/definition, MethodologyBinding, CommandRun, AuditEvent and generic CRM row; never delete or rewrite history.

### Rollback

- 应用级回滚：隐藏 `/g64111` setup UI 与商业 legacy 入口，回退 SAAS-210 业务提交。
- 数据级约束：不得删除已物化的 tenant pack/version/definitions、binding 历史、CommandRun 或 AuditEvent；解绑只移动 Matter active pointer，保留所有通用核心与历史。
- capability/command gate 可保持关闭以阻止新写入；任何生产变更或回滚均需单独批准。

### Completion report

必须按下列格式汇报，并保证 `SELF-CULTIVATION FILES TOUCHED: NONE`、`PRODUCTION TOUCHED: NO`：

```text
THREAD: CRM
BRANCH:
WORKTREE:
TASK: SAAS-210
BASE SHA:
COMMIT:
CHANGED FILES:
TESTS:
REMOTE CI:
SHARED FILES TOUCHED:
SELF-CULTIVATION FILES TOUCHED:
PRODUCTION TOUCHED:
HOMEPAGE CHECK:
NEXT GATE:
```
