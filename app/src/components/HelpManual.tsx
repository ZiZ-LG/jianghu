import { Modal } from './Modal';
import { Markdownish } from './Markdownish';

// 产品内内部手册：只描述已上线的入口与真实失败语义。
const MANUAL = `# 江湖内部版 · 用户手册

> 江湖用来维护大客户、大项目的干系人作战图。以页面的已保存、正在保存或需要重试状态为准。

## 一、作战图

- 先建客户和商机，再把当前商机真正涉及的人加入地图。
- 标注 A 批准人、D 拍板人、U 使用者、R 影响者·技术把关、C 教练和支持度。
- 补充 L1 组织、L2 决策、L3 情感、L4 战略关系，结合 G64111 趋赢力找缺口。
- FORM、BI、交往日志是真人信息，只记录与获授权销售作战有关的内容。

## 二、Workbuddy 同步

- 在 MCP Token 管理中按用途创建只读、调研提议或同步令牌；不再使用时及时撤销。
- 把系统生成的 MCP 配置放进 Workbuddy、OpenClaw 或兼容客户端。令牌只显示一次，不要转发。
- Workbuddy 可同步客户、商机、拜访纪要、候选人物/关系和任务。每次回执分开统计已新建、已更新、已提议、已跳过和失败。
- 客户端超时不代表服务端失败。先核对 SyncReceipt/SyncRun，再用原幂等键重试。

## 三、收件箱与人审

- 机器生成的候选人物、关系、Evidence 和字段修改只进收件箱/提案，不会直接成为正式事实。
- 逐项查看类型、来源、依据和归属后再采纳或拒绝。批量处理会显示每项进度和失败项。
- 只有已审核 Evidence 会进入 PDE 快照；待审和已拒绝内容不改变评估。

## 四、AI 数据范围

- 调用 AI 前会显示服务端核对的人数、关系数和字段类别。
- 默认不发原始日志和 FORM；如确有必要可手动包含。
- 私密 BI、self 日志和非当前商机数据始终排除。AI 分析与候选均需人审，不自动写库。

## 五、纠错、合并和恢复

- 用纠错入口修改客户/商机关键字段，或把拜访、笔记重新绑定到正确客户和商机。
- 重复人物走人物合并流程：选保留人物和重复人物，审阅角色冲突后确认。
- 客户/商机误归档后，由 owner 或 admin 在归档清单恢复。日常界面不提供硬删除整棵客户树。

## 六、失败怎么看

- 只有页面显示已保存才代表服务端已确认。失败时保留草稿，按提示重试或重新登录。
- 冲突后系统会拉取服务端版本再排队保存；仍冲突时先核对同事的修改。
- 页面不承诺毫无延迟的实时同步；以保存状态、收件箱和同步回执为准。`;

export function HelpManual({ onClose }: { onClose: () => void }) {
  return (
    <Modal title="❓ 江湖内部版 · 用户手册" width={680} onClose={onClose}
      footer={<button className="btn primary" onClick={onClose}>我知道了</button>}>
      <Markdownish text={MANUAL} />
    </Modal>
  );
}
